# AI_CBC 技术栈优化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Render/Atlas/Upstash 免费层内提升性能，同时保持代码可随时迁移到个人云服务器上的 Docker Compose 多容器部署。

**Architecture:** 通过 `orjson`、懒加载分析库、Celery 配置优化、真实健康检查、MongoDB 索引、死信队列和 Dockerfile 瘦身，让 Render 单容器里的 API 进程启动更快、Worker 更稳；同一套镜像在迁移后直接拆成 API/Worker/Beat 独立容器。

**Tech Stack:** Python 3.11、FastAPI、Celery、Beanie/Motor、Redis、MongoDB、React/Vite、Cloudflare Pages、Docker。

---

## 文件结构

| 文件 | 用途 |
|------|------|
| `pyproject.toml` | 添加 `orjson` 依赖 |
| `src/aicbc/main.py` | 设置 `default_response_class=ORJSONResponse` |
| `src/aicbc/analysis/tasks.py` | Celery 低命令量配置、死信队列写入 |
| `src/aicbc/analysis/routes.py` | 懒加载重依赖模块 |
| `src/aicbc/monitoring/health.py` | 真实的 MongoDB/Redis ping 检查 |
| `src/aicbc/core/models/db_documents.py` | 新增 `created_at` 索引、死信文档模型 |
| `docker/Dockerfile` | 删除无用依赖、压缩镜像 |
| `.dockerignore` | 排除 `.superpowers/`、`.claude/`、报告目录 |
| `frontend/.env.production` | 指向前端托管时的 API 地址（如需要） |
| `scripts/verify_selfhosted_stack.py` | 迁移后一键验证脚本 |
| `tests/test_celery_integration.py` | 新增 Celery 配置断言 |
| `tests/test_health.py` | 新增真实健康检查测试 |
| `tests/test_api_lazy_imports.py` | 验证 API 不预加载 pandas/numpy |
| `tests/test_analysis_tasks.py` | 新增死信队列测试 |

---

### Task 1: 添加 `orjson` 并切换 FastAPI 默认响应类

**Files:**
- Modify: `pyproject.toml`
- Modify: `src/aicbc/main.py:1-160`
- Test: `tests/test_orjson_response.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_orjson_response.py`：

```python
from fastapi.testclient import TestClient


def test_api_uses_orjson_response():
    from aicbc.main import app

    client = TestClient(app)
    response = client.get("/health")
    assert response.status_code == 200
    assert "application/json" in response.headers["content-type"]
    # ORJSONResponse is a subclass of JSONResponse but uses orjson under the hood.
    # We verify the response class name indirectly via the app config.
    from fastapi.responses import ORJSONResponse

    assert app.default_response_class is ORJSONResponse
```

运行：

```bash
uv run pytest tests/test_orjson_response.py -v
```

Expected: FAIL (`app.default_response_class` is `JSONResponse`)

- [ ] **Step 2: 在 `pyproject.toml` 添加依赖**

在 `[project] dependencies` 列表中加入：

```toml
    "orjson>=3.10.0",
```

- [ ] **Step 3: 修改 `src/aicbc/main.py`**

把 `JSONResponse` 导入改为 `ORJSONResponse`：

```python
from fastapi.responses import ORJSONResponse
```

创建 FastAPI 应用时设置默认响应类：

```python
app = FastAPI(
    title="AI_CBC API",
    description="AI-powered Choice-Based Conjoint virtual consumer research platform",
    version="0.1.0",
    docs_url="/docs" if settings.debug else None,
    redoc_url="/redoc" if settings.debug else None,
    lifespan=lifespan,
    default_response_class=ORJSONResponse,
)
```

全局异常处理也改用 `ORJSONResponse`：

```python
@app.exception_handler(Exception)
async def global_exception_handler(request: Request, exc: Exception) -> ORJSONResponse:
    logger.error("Unhandled exception", exc_info=exc, path=request.url.path)
    if settings.debug:
        return ORJSONResponse(
            status_code=500,
            content={"error": "Internal server error", "detail": str(exc)},
        )
    return ORJSONResponse(
        status_code=500,
        content={"error": "Internal server error"},
    )
```

- [ ] **Step 4: 运行测试**

```bash
uv run pytest tests/test_orjson_response.py -v
```

Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add pyproject.toml src/aicbc/main.py tests/test_orjson_response.py
git commit -m "perf(api): use orjson as default JSON serializer"
```

---

### Task 2: 优化 Celery Redis 命令消耗

**Files:**
- Modify: `src/aicbc/analysis/tasks.py:21-38`
- Test: `tests/test_celery_integration.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_celery_integration.py` 的 `TestCeleryAppConfig` 类中加入：

```python
    def test_result_expires_short(self):
        assert celery_app.conf.result_expires == 300

    def test_task_ignore_result_enabled(self):
        task = celery_app.tasks.get("aicbc.analysis.run_analysis_task")
        assert task.ignore_result is True

    def test_result_extended_disabled(self):
        assert celery_app.conf.result_extended is False
```

运行：

```bash
uv run pytest tests/test_celery_integration.py::TestCeleryAppConfig -v
```

Expected: 3 个 FAIL（配置值不符）

- [ ] **Step 2: 修改 Celery 配置**

在 `src/aicbc/analysis/tasks.py` 中把 `celery_app.conf.update(...)` 改为：

```python
celery_app.conf.update(
    task_serializer="json",
    accept_content=["json"],
    result_serializer="json",
    timezone="UTC",
    enable_utc=True,
    task_track_started=True,
    task_acks_late=True,
    worker_prefetch_multiplier=1,
    result_expires=300,  # Results expire after 5 minutes to reduce Redis memory/commands
    result_extended=False,  # Do not store task name/args in result backend
    task_ignore_result=True,  # Most tasks only need state (PENDING/STARTED/SUCCESS), not return value
)
```

- [ ] **Step 3: 运行测试**

```bash
uv run pytest tests/test_celery_integration.py::TestCeleryAppConfig -v
```

Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add src/aicbc/analysis/tasks.py tests/test_celery_integration.py
git commit -m "perf(celery): reduce redis command usage with short ttl and ignore_result"
```

---

### Task 3: 实现真实的 MongoDB/Redis 健康检查

**Files:**
- Modify: `src/aicbc/monitoring/health.py:45-91`
- Test: `tests/test_health.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_health.py`：

```python
from unittest.mock import AsyncMock, MagicMock, patch

import pytest

from aicbc.monitoring.health import _check_mongodb, _check_redis


@pytest.mark.asyncio
async def test_check_mongodb_pings():
    mock_client = MagicMock()
    mock_client.admin.command = AsyncMock(return_value={"ok": 1})

    with patch("aicbc.main._mongo_client", mock_client):
        result = await _check_mongodb()

    assert result.status == "ok"
    mock_client.admin.command.assert_awaited_once_with("ping")


@pytest.mark.asyncio
async def test_check_mongodb_fail():
    with patch("aicbc.main._mongo_client", None):
        result = await _check_mongodb()

    assert result.status == "fail"


@pytest.mark.asyncio
async def test_check_redis_pings():
    mock_redis = MagicMock()
    mock_redis.ping = AsyncMock(return_value=True)
    mock_redis.close = AsyncMock()

    with patch("redis.asyncio.from_url", return_value=mock_redis):
        with patch("aicbc.config.settings.get_settings") as mock_settings:
            mock_settings.return_value.database.redis_url = "redis://localhost:6379/0"
            result = await _check_redis()

    assert result.status == "ok"
    mock_redis.ping.assert_awaited_once()
    mock_redis.close.assert_awaited_once()
```

运行：

```bash
uv run pytest tests/test_health.py -v
```

Expected: FAIL（当前函数是占位实现）

- [ ] **Step 2: 修改 `src/aicbc/monitoring/health.py`**

把 `_check_mongodb` 改为真实 ping：

```python
async def _check_mongodb() -> DependencyCheck:
    """Check MongoDB connectivity."""
    start = asyncio.get_event_loop().time()
    try:
        from aicbc.main import _mongo_client

        if _mongo_client is None:
            raise RuntimeError("MongoDB client not initialized")
        await _mongo_client.admin.command("ping")
        latency = (asyncio.get_event_loop().time() - start) * 1000
        return DependencyCheck(
            name="mongodb",
            status="ok",
            latency_ms=round(latency, 2),
        )
    except Exception as exc:
        latency = (asyncio.get_event_loop().time() - start) * 1000
        return DependencyCheck(
            name="mongodb",
            status="fail",
            latency_ms=round(latency, 2),
            message=str(exc),
        )
```

把 `_check_redis` 改为真实 ping：

```python
async def _check_redis() -> DependencyCheck:
    """Check Redis connectivity."""
    start = asyncio.get_event_loop().time()
    try:
        import redis.asyncio as aioredis

        settings = get_settings()
        r = aioredis.from_url(settings.database.redis_url, decode_responses=True)
        await r.ping()
        await r.close()
        latency = (asyncio.get_event_loop().time() - start) * 1000
        return DependencyCheck(
            name="redis",
            status="ok",
            latency_ms=round(latency, 2),
        )
    except Exception as exc:
        latency = (asyncio.get_event_loop().time() - start) * 1000
        return DependencyCheck(
            name="redis",
            status="fail",
            latency_ms=round(latency, 2),
            message=str(exc),
        )
```

- [ ] **Step 3: 运行测试**

```bash
uv run pytest tests/test_health.py -v
```

Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add src/aicbc/monitoring/health.py tests/test_health.py
git commit -m "feat(health): implement real mongodb and redis ping checks"
```

---

### Task 4: 为高频集合添加 `created_at` 索引

**Files:**
- Modify: `src/aicbc/core/models/db_documents.py`
- Test: `tests/test_db_indexes.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_db_indexes.py`：

```python
import pytest

from aicbc.core.models.db_documents import (
    ALL_DOCUMENT_MODELS,
    AnalysisJobDocument,
    AnalysisResultDocument,
)


def test_analysis_job_has_created_at_index():
    settings = AnalysisJobDocument.Settings
    index_fields = _extract_index_fields(settings)
    assert ("created_at", -1) in index_fields or "created_at" in index_fields


def test_analysis_result_has_created_at_index():
    settings = AnalysisResultDocument.Settings
    index_fields = _extract_index_fields(settings)
    assert ("created_at", -1) in index_fields or "created_at" in index_fields


def _extract_index_fields(settings):
    names = set()
    for idx in getattr(settings, "indexes", []):
        for field, direction in idx.document["key"].items():
            names.add((field, direction))
    return names
```

运行：

```bash
uv run pytest tests/test_db_indexes.py -v
```

Expected: FAIL（`created_at` 没有索引）

- [ ] **Step 2: 修改文档模型**

在 `src/aicbc/core/models/db_documents.py` 中，为 `AnalysisJobDocument`、`AnalysisResultDocument`、`AuditLogDocument` 添加索引。`pymongo.IndexModel` 已经导入。

`AnalysisJobDocument`：

```python
    class Settings:
        name = "analysis_jobs"
        indexes = [
            IndexModel([("created_at", -1)]),
        ]
```

`AnalysisResultDocument`：

```python
    class Settings:
        name = "analysis_results"
        indexes = [
            IndexModel([("created_at", -1)]),
        ]
```

`AuditLogDocument`：

```python
    class Settings:
        name = "audit_logs"
        indexes = [
            IndexModel([("created_at", -1)]),
        ]
```

- [ ] **Step 3: 运行测试**

```bash
uv run pytest tests/test_db_indexes.py -v
```

Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add src/aicbc/core/models/db_documents.py tests/test_db_indexes.py
git commit -m "perf(db): add created_at indexes for analysis and audit collections"
```

---

### Task 5: API 路由懒加载重依赖

**Files:**
- Modify: `src/aicbc/analysis/routes.py:1-51`
- Test: `tests/test_api_lazy_imports.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_api_lazy_imports.py`：

```python
import subprocess
import sys
from pathlib import Path


def test_main_import_does_not_load_analysis_heavy_deps():
    repo_root = Path(__file__).resolve().parents[1]
    code = """
import sys
sys.path.insert(0, 'src')
import aicbc.main
assert 'pandas' not in sys.modules, 'pandas loaded at startup'
assert 'numpy' not in sys.modules, 'numpy loaded at startup'
print('ok')
"""
    result = subprocess.run(
        [sys.executable, "-c", code],
        capture_output=True,
        text=True,
        cwd=repo_root,
    )
    assert result.returncode == 0, result.stderr
```

运行：

```bash
uv run pytest tests/test_api_lazy_imports.py -v
```

Expected: FAIL（pandas/numpy 在导入 main 时被加载）

- [ ] **Step 2: 移除顶层重依赖导入**

在 `src/aicbc/analysis/routes.py` 顶部保留以下导入：

```python
from __future__ import annotations

import json
import uuid
from datetime import UTC, datetime

import structlog
from fastapi import APIRouter, Depends, HTTPException, Query, Response, status

from aicbc.analysis.models import (
    AnalysisJobStatus,
    AnalysisResultResponse,
    AnalyzeRequest,
    ConvergenceDiagnostics,
    ImportanceResponse,
    LatentClassRequest,
    LatentClassResponse,
    MarketSimRequest,
    MarketSimResponse,
    ParseScenarioRequest,
    PerAttributeTest,
    ProductScenario,
    ScenarioShare,
    SegmentComparisonResponse,
    WTPResponse,
)
from aicbc.analysis.store import AnalysisStore, get_analysis_store
from aicbc.core.store import get_questionnaire_store, get_response_store
from aicbc.questionnaire.models import Attribute
from aicbc.questionnaire.response_models import CBCRawDataset
```

移除以下顶层导入（改为函数内导入）：

```python
# 从顶部删除这些行
from aicbc.analysis.cbc_visualizer import (...)
from aicbc.analysis.nl_scenario_parser import parse_nl_scenario
from aicbc.analysis.preprocessing import validate_dataset
from aicbc.analysis.report_builder import build_report
from aicbc.analysis.results.segment_comparison import compare_segments as seg_compare
from aicbc.analysis.simulation.market_simulator import MarketSimulator
from aicbc.analysis.tasks import run_analysis_task, run_latent_class_task
```

- [ ] **Step 3: 在函数内部导入**

在 `analyze_study` 函数开头：

```python
    from aicbc.analysis.preprocessing import validate_dataset
    from aicbc.analysis.tasks import run_analysis_task, run_latent_class_task
```

在 `simulate_market` 函数开头：

```python
    from aicbc.analysis.simulation.market_simulator import MarketSimulator
```

在 `compare_segments` 函数开头：

```python
    from aicbc.analysis.results.segment_comparison import compare_segments as seg_compare
```

在 `parse_scenario` 函数开头：

```python
    from aicbc.analysis.nl_scenario_parser import parse_nl_scenario
```

在 `download_report` 函数开头：

```python
    from aicbc.analysis.report_builder import build_report
```

在 `get_visualization` 函数开头：

```python
    from aicbc.analysis.cbc_visualizer import (
        build_dashboard_option,
        build_importance_chart_option,
        build_importance_pie_option,
        build_market_share_option,
        build_utility_distribution_option,
        build_wtp_chart_option,
    )
```

- [ ] **Step 4: 运行测试**

```bash
uv run pytest tests/test_api_lazy_imports.py tests/test_api_analysis_jobs.py -v
```

Expected: PASS（默认测试不跑慢测试，分析任务测试会被 mock）

- [ ] **Step 5: 提交**

```bash
git add src/aicbc/analysis/routes.py tests/test_api_lazy_imports.py
git commit -m "perf(routes): lazy-load heavy analysis modules in api process"
```

---

### Task 6: Worker 失败时写入死信队列

**Files:**
- Modify: `src/aicbc/core/models/db_documents.py`
- Modify: `src/aicbc/analysis/tasks.py`
- Test: `tests/test_analysis_tasks.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_analysis_tasks.py` 的 `TestRunAnalysisTask` 中加入：

```python
    @patch("aicbc.analysis.engines.hb_engine.HBEngine")
    @patch("aicbc.analysis.engines.hb_engine.HBConfig")
    def test_dead_letter_saved_on_failure(
        self,
        mock_hb_config_cls,
        mock_hb_engine_cls,
        mock_stores,
        mock_preprocessing,
    ):
        from aicbc.core.models.db_documents import DeadLetterDocument

        mock_hb_engine_cls.return_value.fit.side_effect = RuntimeError("boom")

        captured = []

        async def capture_insert(self):
            captured.append(self)

        with patch.object(DeadLetterDocument, "insert", side_effect=capture_insert):
            with pytest.raises(RuntimeError):
                run_analysis_task(
                    "study-001",
                    "analysis-001",
                    "hb",
                    json.dumps({}),
                )

        assert len(captured) == 1
        doc = captured[0]
        assert doc.task_name == "aicbc.analysis.run_analysis_task"
        assert doc.analysis_id == "analysis-001"
        assert "RuntimeError: boom" in doc.exception
```

运行：

```bash
uv run pytest tests/test_analysis_tasks.py::TestRunAnalysisTask::test_dead_letter_saved_on_failure -v
```

Expected: FAIL（`DeadLetterDocument` 不存在）

- [ ] **Step 2: 添加死信文档模型**

在 `src/aicbc/core/models/db_documents.py` 中加入：

```python
class DeadLetterDocument(Document):
    """Persisted record of failed Celery tasks for later inspection."""

    task_name: str
    analysis_id: str | None = None
    study_id: str | None = None
    exception: str
    created_at: datetime = Field(default_factory=lambda: datetime.now(UTC))

    class Settings:
        name = "dead_letters"
        indexes = [
            IndexModel([("created_at", -1)]),
            IndexModel([("task_name", 1)]),
        ]
```

并把 `DeadLetterDocument` 加入 `ALL_DOCUMENT_MODELS` 列表末尾。

- [ ] **Step 3: 修改任务异常处理**

在 `src/aicbc/analysis/tasks.py` 中新增一个同步 helper：

```python
def _save_dead_letter(
    task_name: str,
    analysis_id: str | None,
    study_id: str | None,
    exception: Exception,
) -> None:
    """Persist a dead-letter record for a failed task."""
    import asyncio
    import traceback

    from aicbc.core.models.db_documents import DeadLetterDocument

    async def _insert() -> None:
        doc = DeadLetterDocument(
            task_name=task_name,
            analysis_id=analysis_id,
            study_id=study_id,
            exception=f"{type(exception).__name__}: {exception}",
        )
        await doc.insert()

    try:
        asyncio.run(_insert())
    except Exception:
        log.exception("dead_letter_insert_failed")
```

把 `run_analysis_task` 和 `run_latent_class_task` 的 `except Exception:` 块改为：

```python
    except Exception as exc:
        log.exception("analysis_task_failed")
        analysis_store.update_job_status(analysis_id, "FAILED", progress=0.0)
        _save_dead_letter(
            task_name="aicbc.analysis.run_analysis_task",
            analysis_id=analysis_id,
            study_id=study_id,
            exception=exc,
        )
        raise
```

对 `run_latent_class_task` 使用 `task_name="aicbc.analysis.run_latent_class_task"`。

- [ ] **Step 4: 运行测试**

```bash
uv run pytest tests/test_analysis_tasks.py -v
```

Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/aicbc/core/models/db_documents.py src/aicbc/analysis/tasks.py tests/test_analysis_tasks.py
git commit -m "feat(worker): persist failed celery tasks to dead_letters collection"
```

---

### Task 7: 压缩 Dockerfile 镜像

**Files:**
- Modify: `docker/Dockerfile`
- Modify: `.dockerignore`
- Test: 本地构建并查看镜像大小

- [ ] **Step 1: 修改 Dockerfile**

当前 builder 阶段安装 `libpq-dev` 用不到，删除。把 builder stage 改为：

```dockerfile
# Stage 1: Builder — install dependencies
FROM python:3.11-slim AS builder

WORKDIR /app

# Install build dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# Install Python dependencies
# Note: uv.lock is gitignored, so install directly from pyproject.toml.
# Include [analysis] extras because Celery worker needs pandas/numpy/pymc.
COPY pyproject.toml ./
COPY src/ ./src/
ENV UV_NO_CACHE=1
RUN pip install --no-cache-dir uv && \
    uv pip install --system --no-cache -e ".[analysis]"

# Stage 2: Runtime — minimal image
FROM python:3.11-slim AS runtime

WORKDIR /app

# Create non-root user for security
RUN groupadd -r aicbc && useradd -r -g aicbc aicbc

# Install supervisord for multi-process single container (Render free plan only supports one web service)
RUN apt-get update && apt-get install -y --no-install-recommends \
    supervisor \
    && rm -rf /var/lib/apt/lists/*

# Copy installed packages from builder
COPY --from=builder /usr/local/lib/python3.11/site-packages /usr/local/lib/python3.11/site-packages
COPY --from=builder /usr/local/bin /usr/local/bin

# Copy application code
COPY --chown=aicbc:aicbc src/ ./src/
COPY --chown=aicbc:aicbc configs/ ./configs/

# Copy supervisord configuration
COPY docker/supervisord-render.conf /etc/supervisor/conf.d/supervisord.conf

# Pre-compile bytecode to reduce cold-start import time on Render free tier
RUN python -m compileall -q /app/src /app/configs

# Remove write permissions for security
RUN chmod -R 555 /app/src /app/configs

EXPOSE 8000

# Health check
HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \
    CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')" || exit 1

CMD ["/usr/bin/supervisord", "-c", "/etc/supervisor/conf.d/supervisord.conf"]
```

- [ ] **Step 2: 更新 `.dockerignore`**

在 `.dockerignore` 中加入：

```text
# Superpowers / local tooling
.superpowers/
.claude/

# Reports (not needed in image)
reports/
```

- [ ] **Step 3: 构建并查看大小**

```bash
docker build -f docker/Dockerfile -t aicbc:optimized .
docker images aicbc:optimized --format "{{.Size}}"
```

Expected: 构建成功，镜像大小不比之前大

- [ ] **Step 4: 提交**

```bash
git add docker/Dockerfile .dockerignore
git commit -m "perf(docker): slim image and remove unused libpq-dev"
```

---

### Task 8: 前端生产环境配置检查

**Files:**
- Modify: `frontend/.env.production`（如域名变化）
- Test: `frontend/` 构建

- [ ] **Step 1: 确认/更新 API 地址**

如果 Render API 域名不是 `https://aicbc-api.fromworldimport.com`，修改 `frontend/.env.production`：

```text
VITE_API_BASE_URL=https://<your-render-api>/api/v1
VITE_ROOT_API_BASE_URL=https://<your-render-api>
VITE_API_KEY=dev-key-change-in-prod
```

- [ ] **Step 2: 构建验证**

```bash
cd frontend
npm ci
npm run build
```

Expected: `dist/` 目录生成，无错误

- [ ] **Step 3: 提交**

```bash
git add frontend/.env.production
git commit -m "chore(frontend): update production api endpoint for cloudflare pages"
```

---

### Task 9: 添加自托管迁移验证脚本

**Files:**
- Create: `scripts/verify_selfhosted_stack.py`
- Test: 本地 `docker compose up` 后运行

- [ ] **Step 1: 创建脚本**

创建 `scripts/verify_selfhosted_stack.py`：

```python
"""Verify a self-hosted AI_CBC stack after `docker compose up`."""

from __future__ import annotations

import argparse
import sys

import httpx


def check(base_url: str, path: str, expected_status: int = 200) -> bool:
    url = f"{base_url}{path}"
    try:
        response = httpx.get(url, timeout=10.0)
    except httpx.RequestError as exc:
        print(f"FAIL {url}: {exc}")
        return False

    ok = response.status_code == expected_status
    status = "PASS" if ok else "FAIL"
    print(f"{status} {url} -> {response.status_code}")
    return ok


def main() -> int:
    parser = argparse.ArgumentParser()
    parser.add_argument("--base-url", default="http://localhost")
    args = parser.parse_args()

    results = [
        check(args.base_url, "/health"),
        check(args.base_url, "/ready"),
    ]

    if all(results):
        print("Stack verification passed")
        return 0
    print("Stack verification failed")
    return 1


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: 本地验证**

```bash
docker compose up -d
uv run python scripts/verify_selfhosted_stack.py --base-url http://localhost
```

Expected: 2 个 PASS

- [ ] **Step 3: 提交**

```bash
git add scripts/verify_selfhosted_stack.py
git commit -m "feat(ops): add self-hosted stack verification script"
```

---

### Task 10: 更新 Celery 集成测试

**Files:**
- Modify: `tests/test_celery_integration.py`

- [ ] **Step 1: 在 Redis 集成测试类中补充 AsyncResult 状态仍可追踪的测试**

在 `TestCeleryRedisIntegration` 中加入：

```python
    def test_async_result_state_still_works_with_ignore_result(self):
        """Even with task_ignore_result, the broker still tracks task state."""
        result = run_analysis_task.apply_async(
            args=["study-test-002", "analysis-test-002", "mnl", "{}"],
            countdown=3600,
        )
        assert result.id is not None
        # State should be PENDING immediately after enqueue.
        assert result.state in ("PENDING",)
        result.revoke(terminate=False)
```

- [ ] **Step 2: 运行测试**

```bash
uv run pytest tests/test_celery_integration.py -v
```

Expected: PASS（`TestCeleryAppConfig` 始终运行；Redis 不可用时 `TestCeleryRedisIntegration` 被跳过）

- [ ] **Step 3: 提交**

```bash
git add tests/test_celery_integration.py
git commit -m "test(celery): add ignore_result and asyncresult state coverage"
```

---

### Task 11: 运行完整质量门禁

- [ ] **Step 1: 运行 ruff**

```bash
uv run ruff check src/ tests/
uv run ruff format --check src/ tests/
```

Expected: 无错误

- [ ] **Step 2: 运行 mypy**

```bash
uv run mypy src/ --ignore-missing-imports || true
```

Expected: 由于存在历史遗留问题，允许非阻塞；确保新增代码没有引入新的类型错误

- [ ] **Step 3: 运行 pytest**

```bash
uv run pytest tests/ -m "not slow" --cov=src --cov-report=term-missing --cov-fail-under=60
```

Expected: PASS，覆盖率 ≥ 60%

- [ ] **Step 4: 运行前端测试**

```bash
cd frontend
npm run lint
npm run test:coverage
```

Expected: PASS

- [ ] **Step 5: 提交**

```bash
git commit -m "chore: pass quality gates after tech stack optimization"
```

---

### Task 12: 更新文档索引

- [ ] **Step 1: 在 `docs/文档索引与导航.md` 的文档总览表中添加**

```markdown
| 40 | `docs/superpowers/plans/2026-06-17-技术栈优化.md` | 横切 | 小维+小端 | 技术栈优化实施计划 |
```

- [ ] **Step 2: 提交**

```bash
git add "docs/文档索引与导航.md"
git commit -m "docs: register tech stack optimization plan in index"
```

---

## Spec 覆盖检查

| 设计文档章节 | 对应任务 |
|--------------|----------|
| 七、性能优化点（orjson、懒加载） | Task 1, Task 5 |
| 六、Redis 使用策略 | Task 2, Task 10 |
| 八、错误处理（真实健康检查） | Task 3 |
| 七、MongoDB 优化 | Task 4 |
| 八、错误处理（死信队列） | Task 6 |
| 七、容器瘦身 | Task 7 |
| 前端迁到 Cloudflare Pages | Task 8 |
| 迁移 Checklist | Task 9 |

无遗漏。

## Placeholder 检查

- 无 TBD/TODO/实现后再说
- 所有代码块包含可直接运行的内容
- 所有测试包含具体断言
- 所有文件路径使用绝对仓库路径
