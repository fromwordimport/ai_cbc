# AI_CBC 后端性能优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Azure B2ats v2 双机部署约束下，通过建立性能基准、补齐可观测性、对测量出的瓶颈做低风险代码优化，最终提升 API 响应速度并降低资源占用。

**Architecture:** 保持现有 FastAPI + Motor/Beanie + Celery + Redis 架构不变；新增基准脚本与 Prometheus/Alertmanager/Grafana 规则；将 Dashboard 聚合、画像批量生成、LLM 异步调用、MongoDB 查询投影、缓存策略等热点逐个优化；所有改动均可通过基准脚本前后对比验证。

**Tech Stack:** Python 3.11, FastAPI, Pydantic v2, Motor, Beanie, Celery, Redis, Prometheus, Grafana, httpx, pytest.

---

## File Structure Map

| File | Responsibility |
|------|----------------|
| `scripts/benchmark_api.py` | 压测高频 API，输出 RPS/P50/P95/P99/错误率 |
| `scripts/benchmark_worker.py` | 触发并监控分析任务，记录排队/执行时间 |
| `scripts/profile_endpoint.py` | 单端点火焰图采集（py-spy 包装） |
| `scripts/benchmark_image.py` | 测量镜像体积与冷启动时间 |
| `src/aicbc/config/settings.py` | 新增 MongoDB/LLM/缓存/优雅关闭环境变量 |
| `.env.example` | 新增环境变量模板 |
| `src/aicbc/core/store_mongo.py` | 新增研究聚合/投影方法 |
| `src/aicbc/core/models/db_documents.py` | 新增 `PersonaGenerationJobDocument` |
| `src/aicbc/monitoring/health.py` | Dashboard summary 改为聚合查询 + 缓存 |
| `src/aicbc/agents/consumer_generator.py` | 抽离 async batch core，支持 Celery loop |
| `src/aicbc/analysis/tasks.py` | 新增 `run_persona_generation_task` Celery 任务 |
| `src/aicbc/api/routes/personas.py` | 新增异步批量生成入口与状态轮询端点 |
| `src/aicbc/llm/client.py` | 新增 `httpx.AsyncClient` 异步调用路径 |
| `src/aicbc/core/cache.py` | 扩展 `TimedCache` 支持可选 Redis 后端 |
| `src/aicbc/main.py` | 中间件顺序、GZip 级别、优雅关闭 |
| `src/aicbc/monitoring/metrics.py` | 新增画像生成/缓存/MongoDB 指标 |
| `docker/prometheus-rules.yml` | Prometheus recording rules |
| `docker/alertmanager.yml` | Alertmanager 路由与告警规则 |
| `docker/prometheus.yml` | 挂载 rules 目录与 alertmanager |
| `docker/grafana/dashboards/aicbc-dashboard.json` | 扩展 Grafana 面板 |
| `docker/Dockerfile` | 新增 `api`/`worker` target（不启用） |
| `docker-compose.yml` | 补全 beat/mongo/redis 资源限制 |
| `docker-compose.azure-b2ats.yml` | 同步新环境变量与资源限制 |
| `k8s/base/configmap.yaml` | 新增配置项与注释 |
| `.github/workflows/ci.yml` | 新增 `performance-baseline` job |
| `tests/test_*` | 新增/更新单元与集成测试 |

---

## Phase 1: 基准与监控

### Task 1: 创建 API 基准脚本

**Files:**
- Create: `scripts/benchmark_api.py`
- Test: `scripts/benchmark_api.py`（自验证运行）

- [ ] **Step 1: 编写脚本**

```python
"""API benchmark script for AI_CBC."""

from __future__ import annotations

import argparse
import asyncio
import statistics
import time
from dataclasses import dataclass, field
from typing import Any

import httpx


@dataclass
class BenchmarkResult:
    endpoint: str
    status_codes: list[int] = field(default_factory=list)
    latencies: list[float] = field(default_factory=list)
    errors: list[str] = field(default_factory=list)

    @property
    def total(self) -> int:
        return len(self.latencies) + len(self.errors)

    @property
    def rps(self) -> float:
        if not self.latencies:
            return 0.0
        duration = max(self.latencies) - min(self.latencies) if len(self.latencies) > 1 else self.latencies[0]
        return len(self.latencies) / max(duration, 0.001)

    def percentile(self, p: float) -> float:
        if not self.latencies:
            return 0.0
        sorted_lat = sorted(self.latencies)
        k = (len(sorted_lat) - 1) * p / 100
        f = int(k)
        c = min(f + 1, len(sorted_lat) - 1)
        return sorted_lat[f] + (k - f) * (sorted_lat[c] - sorted_lat[f])


async def _worker(
    client: httpx.AsyncClient,
    base_url: str,
    method: str,
    path: str,
    body: dict[str, Any] | None,
    requests: int,
    results: BenchmarkResult,
) -> None:
    for _ in range(requests):
        start = time.perf_counter()
        try:
            if method == "GET":
                response = await client.get(f"{base_url}{path}", timeout=30.0)
            else:
                response = await client.post(f"{base_url}{path}", json=body, timeout=30.0)
            results.status_codes.append(response.status_code)
            results.latencies.append(time.perf_counter() - start)
        except Exception as exc:
            results.errors.append(str(exc))


async def run_benchmark(
    base_url: str,
    endpoint: str,
    method: str,
    path: str,
    body: dict[str, Any] | None,
    concurrency: int,
    requests_per_worker: int,
) -> BenchmarkResult:
    results = BenchmarkResult(endpoint=endpoint)
    async with httpx.AsyncClient(limits=httpx.Limits(max_connections=concurrency * 2)) as client:
        await asyncio.gather(
            *[
                _worker(client, base_url, method, path, body, requests_per_worker, results)
                for _ in range(concurrency)
            ]
        )
    return results


def print_report(results: list[BenchmarkResult]) -> None:
    print(f"{'Endpoint':<30} {'Total':>8} {'OK':>8} {'Err':>8} {'RPS':>10} {'P50(ms)':>10} {'P95(ms)':>10} {'P99(ms)':>10}")
    for r in results:
        ok = sum(1 for c in r.status_codes if 200 <= c < 300)
        print(
            f"{r.endpoint:<30} {r.total:>8} {ok:>8} {len(r.errors):>8} "
            f"{r.rps:>10.2f} {r.percentile(50) * 1000:>10.1f} {r.percentile(95) * 1000:>10.1f} {r.percentile(99) * 1000:>10.1f}"
        )


async def main() -> None:
    parser = argparse.ArgumentParser(description="Benchmark AI_CBC API endpoints")
    parser.add_argument("--base-url", default="http://localhost:8000", help="API base URL")
    parser.add_argument("--duration", type=int, default=60, help="Duration per scenario in seconds")
    parser.add_argument("--output", default="reports/performance/benchmark_api.json", help="Output JSON path")
    args = parser.parse_args()

    scenarios = [
        ("health", "GET", "/health", None, 50),
        ("ready", "GET", "/ready", None, 20),
        ("dashboard_summary", "GET", "/dashboard/summary", None, 50),
        ("studies", "GET", "/api/v1/studies", None, 20),
        ("personas", "GET", "/api/v1/personas", None, 20),
    ]

    results: list[BenchmarkResult] = []
    for name, method, path, body, concurrency in scenarios:
        requests_per_worker = max(1, args.duration * concurrency // 10)
        print(f"Running {name}: concurrency={concurrency}, total_requests={requests_per_worker * concurrency}")
        result = await run_benchmark(args.base_url, name, method, path, body, concurrency, requests_per_worker)
        results.append(result)

    print_report(results)

    import json
    from pathlib import Path

    output_path = Path(args.output)
    output_path.parent.mkdir(parents=True, exist_ok=True)
    output_path.write_text(
        json.dumps(
            [
                {
                    "endpoint": r.endpoint,
                    "total": r.total,
                    "errors": len(r.errors),
                    "rps": r.rps,
                    "p50_ms": r.percentile(50) * 1000,
                    "p95_ms": r.percentile(95) * 1000,
                    "p99_ms": r.percentile(99) * 1000,
                }
                for r in results
            ],
            indent=2,
        ),
        encoding="utf-8",
    )
    print(f"Report written to {output_path}")


if __name__ == "__main__":
    asyncio.run(main())
```

- [ ] **Step 2: 运行脚本验证无语法错误**

Run: `uv run python scripts/benchmark_api.py --base-url http://localhost:8000 --duration 5`
Expected: 若服务未启动则部分请求失败属正常；脚本本身无异常退出。

- [ ] **Step 3: Commit**

```bash
git add scripts/benchmark_api.py
git commit -m "perf(scripts): add API benchmark script for RPS and latency"
```

---

### Task 2: 创建 Worker 基准脚本

**Files:**
- Create: `scripts/benchmark_worker.py`
- Test: `scripts/benchmark_worker.py`

- [ ] **Step 1: 编写脚本**

```python
"""Celery worker benchmark for analysis tasks."""

from __future__ import annotations

import argparse
import json
import time
from datetime import UTC, datetime
from pathlib import Path

import httpx


def poll_task_status(base_url: str, analysis_id: str, timeout: int = 600) -> dict:
    start = time.perf_counter()
    with httpx.Client() as client:
        while time.perf_counter() - start < timeout:
            response = client.get(f"{base_url}/api/v1/analysis/{analysis_id}/status", timeout=10.0)
            response.raise_for_status()
            data = response.json()
            if data.get("status") in ("COMPLETED", "FAILED", "TIMED_OUT"):
                return {"final": data, "elapsed": time.perf_counter() - start}
            time.sleep(2.0)
    raise TimeoutError(f"Task {analysis_id} did not complete within {timeout}s")


def main() -> None:
    parser = argparse.ArgumentParser(description="Benchmark Celery analysis worker")
    parser.add_argument("--base-url", default="http://localhost:8000")
    parser.add_argument("--study-id", default="benchmark-worker-study")
    parser.add_argument("--output", default="reports/performance/benchmark_worker.json")
    args = parser.parse_args()

    base = args.base_url.rstrip("/")
    report: dict = {
        "started_at": datetime.now(UTC).isoformat(),
        "study_id": args.study_id,
    }

    with httpx.Client() as client:
        # 1. Create study
        client.post(
            f"{base}/api/v1/studies",
            json={
                "study_id": args.study_id,
                "product_category": "dishwasher",
                "research_goal": "benchmark",
            },
            timeout=10.0,
        )

        # 2. Upload a minimal dataset (150 respondents x 12 choice sets x 3 alts)
        # For benchmark purposes we generate a deterministic synthetic dataset.
        n_respondents = 150
        n_choice_sets = 12
        n_alternatives = 3
        choice_records = []
        for r in range(n_respondents):
            for cs in range(n_choice_sets):
                choice_records.append(
                    {
                        "respondent_id": f"resp-{r:03d}",
                        "choice_set_id": cs,
                        "selected_alternative": r % n_alternatives,
                        "available_alternatives": list(range(n_alternatives)),
                    }
                )

        dataset_payload = {
            "study_id": args.study_id,
            "n_respondents": n_respondents,
            "n_choice_sets": n_choice_sets,
            "n_alternatives": n_alternatives,
            "choice_records": choice_records,
        }
        client.post(f"{base}/api/v1/studies/{args.study_id}/dataset", json=dataset_payload, timeout=30.0)

        # 3. Trigger HB analysis
        analysis_id = f"{args.study_id}-hb"
        response = client.post(
            f"{base}/api/v1/analysis",
            json={
                "study_id": args.study_id,
                "analysis_id": analysis_id,
                "model_type": "hb",
                "config": {"n_draws": 500, "n_tune": 500, "n_chains": 2, "n_cores": 1},
            },
            timeout=10.0,
        )
        response.raise_for_status()
        queued_at = time.perf_counter()

        # 4. Poll to completion
        result = poll_task_status(base, analysis_id)
        report["queued_at"] = queued_at
        report["total_elapsed_seconds"] = result["elapsed"]
        report["status"] = result["final"].get("status")

    output_path = Path(args.output)
    output_path.parent.mkdir(parents=True, exist_ok=True)
    output_path.write_text(json.dumps(report, indent=2), encoding="utf-8")
    print(json.dumps(report, indent=2))


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 运行验证语法**

Run: `uv run python scripts/benchmark_worker.py --base-url http://localhost:8000 --output /tmp/benchmark_worker.json`
Expected: 若 dataset endpoint 不存在会 404，但脚本无内部异常退出。

- [ ] **Step 3: Commit**

```bash
git add scripts/benchmark_worker.py
git commit -m "perf(scripts): add Celery worker benchmark for HB tasks"
```

---

### Task 3: 创建端点画像与镜像基线脚本

**Files:**
- Create: `scripts/profile_endpoint.py`
- Create: `scripts/benchmark_image.py`

- [ ] **Step 1: 编写 profile_endpoint.py**

```python
"""Profile a single endpoint with py-spy and output a flamegraph."""

from __future__ import annotations

import argparse
import subprocess
import sys
import time
from pathlib import Path

import httpx


def main() -> None:
    parser = argparse.ArgumentParser(description="Profile an endpoint with py-spy")
    parser.add_argument("--base-url", default="http://localhost:8000")
    parser.add_argument("--endpoint", default="/dashboard/summary")
    parser.add_argument("--duration", type=int, default=30)
    parser.add_argument("--output", default="reports/performance/flamegraph.svg")
    args = parser.parse_args()

    output_path = Path(args.output)
    output_path.parent.mkdir(parents=True, exist_ok=True)

    # Find uvicorn process
    proc = subprocess.run(
        ["pgrep", "-f", "uvicorn src.aicbc.main:app"],
        capture_output=True,
        text=True,
        check=False,
    )
    pid = proc.stdout.strip().splitlines()[0] if proc.returncode == 0 else None
    if not pid:
        print("Could not find uvicorn process; is the server running?", file=sys.stderr)
        sys.exit(1)

    # Start py-spy record
    spy_cmd = [
        "py-spy",
        "record",
        "-p",
        pid,
        "-o",
        str(output_path),
        "-d",
        str(args.duration),
        "--rate",
        "100",
    ]
    spy_proc = subprocess.Popen(spy_cmd)

    # Generate load concurrently
    start = time.perf_counter()
    with httpx.Client() as client:
        while time.perf_counter() - start < args.duration:
            try:
                client.get(f"{args.base_url}{args.endpoint}", timeout=10.0)
            except Exception:
                pass
            time.sleep(0.1)

    spy_proc.wait()
    print(f"Flamegraph saved to {output_path}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 编写 benchmark_image.py**

```python
"""Measure Docker image size and cold-start time to /health."""

from __future__ import annotations

import argparse
import json
import subprocess
import time
from pathlib import Path


def run(cmd: list[str]) -> str:
    return subprocess.run(cmd, capture_output=True, text=True, check=True).stdout


def main() -> None:
    parser = argparse.ArgumentParser(description="Benchmark Docker image size and startup")
    parser.add_argument("--tag", default="aicbc:benchmark")
    parser.add_argument("--output", default="reports/performance/benchmark_image.json")
    args = parser.parse_args()

    # Build image
    print(f"Building {args.tag} ...")
    start = time.perf_counter()
    run(["docker", "build", "-f", "docker/Dockerfile", "-t", args.tag, "."])
    build_time = time.perf_counter() - start

    # Inspect image size (bytes)
    inspect = run(["docker", "inspect", "-f", "{{.Size}}", args.tag]).strip()
    image_size_bytes = int(inspect)

    # Cold-start test: run container and time to /health
    print("Measuring cold-start time ...")
    container = subprocess.run(
        ["docker", "run", "-d", "-p", "8000:8000", "--name", "aicbc-benchmark", args.tag],
        capture_output=True,
        text=True,
        check=True,
    ).stdout.strip()

    health_url = "http://localhost:8000/health"
    start = time.perf_counter()
    while time.perf_counter() - start < 120:
        probe = subprocess.run(
            ["curl", "-sf", health_url],
            capture_output=True,
            text=True,
        )
        if probe.returncode == 0:
            break
        time.sleep(0.5)
    cold_start = time.perf_counter() - start

    subprocess.run(["docker", "stop", "aicbc-benchmark"], check=False)
    subprocess.run(["docker", "rm", "aicbc-benchmark"], check=False)

    report = {
        "image_tag": args.tag,
        "image_size_bytes": image_size_bytes,
        "image_size_mb": round(image_size_bytes / 1024 / 1024, 2),
        "build_time_seconds": round(build_time, 2),
        "cold_start_seconds": round(cold_start, 2),
    }
    output_path = Path(args.output)
    output_path.parent.mkdir(parents=True, exist_ok=True)
    output_path.write_text(json.dumps(report, indent=2), encoding="utf-8")
    print(json.dumps(report, indent=2))


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 验证脚本语法**

Run:
```bash
uv run python -m py_compile scripts/profile_endpoint.py
uv run python -m py_compile scripts/benchmark_image.py
```
Expected: 无输出（编译成功）。

- [ ] **Step 4: Commit**

```bash
git add scripts/profile_endpoint.py scripts/benchmark_image.py
git commit -m "perf(scripts): add endpoint profiling and image baseline scripts"
```

---

### Task 4: 新增业务指标埋点

**Files:**
- Modify: `src/aicbc/monitoring/metrics.py`
- Test: `tests/test_metrics.py`

- [ ] **Step 1: 在 `_get_metrics()` 中新增指标**

在 `src/aicbc/monitoring/metrics.py` 的 `_get_metrics()` 返回的 dict 中追加以下键：

```python
"persona_generation_duration_seconds": Histogram(
    "aicbc_persona_generation_duration_seconds",
    "Duration of a persona generation Celery task",
    ["study_id"],
    buckets=[1, 5, 10, 30, 60, 120, 300, 600, 1800],
),
"persona_generation_batch_size": Histogram(
    "aicbc_persona_generation_batch_size",
    "Number of personas requested per batch task",
    buckets=[1, 5, 10, 25, 50, 100],
),
"cache_hit_ratio": Gauge(
    "aicbc_cache_hit_ratio",
    "Cache hit ratio for the active cache backend",
    ["cache_name"],
),
"mongodb_query_duration_seconds": Histogram(
    "aicbc_mongodb_query_duration_seconds",
    "MongoDB query duration",
    ["collection", "operation"],
    buckets=[0.001, 0.005, 0.01, 0.05, 0.1, 0.5, 1, 2, 5],
),
```

- [ ] **Step 2: 新增记录函数**

在文件末尾追加：

```python
def record_persona_generation_task(
    study_id: str,
    duration_seconds: float,
    batch_size: int,
) -> None:
    """Record persona generation Celery task metrics."""
    metrics = _get_metrics()
    metrics["persona_generation_duration_seconds"].labels(study_id=study_id).observe(duration_seconds)
    metrics["persona_generation_batch_size"].observe(batch_size)


def record_cache_hit_ratio(cache_name: str, hit_ratio: float) -> None:
    """Record cache hit ratio (0.0-1.0)."""
    metrics = _get_metrics()
    metrics["cache_hit_ratio"].labels(cache_name=cache_name).set(hit_ratio)


def record_mongodb_query_duration(
    collection: str,
    operation: str,
    duration_seconds: float,
) -> None:
    """Record MongoDB query duration."""
    metrics = _get_metrics()
    metrics["mongodb_query_duration_seconds"].labels(
        collection=collection, operation=operation
    ).observe(duration_seconds)
```

- [ ] **Step 3: 编写测试**

```python
"""Tests for monitoring metrics."""

from aicbc.monitoring.metrics import (
    record_cache_hit_ratio,
    record_mongodb_query_duration,
    record_persona_generation_task,
)


def test_record_persona_generation_task() -> None:
    record_persona_generation_task("study-1", 12.5, 10)


def test_record_cache_hit_ratio() -> None:
    record_cache_hit_ratio("dashboard", 0.85)


def test_record_mongodb_query_duration() -> None:
    record_mongodb_query_duration("personas", "find", 0.025)
```

- [ ] **Step 4: 运行测试**

Run: `uv run pytest tests/test_metrics.py -v`
Expected: 3 passed。

- [ ] **Step 5: Commit**

```bash
git add src/aicbc/monitoring/metrics.py tests/test_metrics.py
git commit -m "perf(monitoring): add persona generation, cache, and MongoDB metrics"
```

---

### Task 5: 创建 Prometheus Recording Rules

**Files:**
- Create: `docker/prometheus-rules.yml`
- Modify: `docker/prometheus.yml`

- [ ] **Step 1: 编写规则文件**

```yaml
groups:
  - name: aicbc_performance
    interval: 15s
    rules:
      - record: aicbc:api_request_p95_seconds
        expr: histogram_quantile(0.95, rate(aicbc_api_request_duration_seconds_bucket[5m]))
      - record: aicbc:api_request_p99_seconds
        expr: histogram_quantile(0.99, rate(aicbc_api_request_duration_seconds_bucket[5m]))
      - record: aicbc:api_requests_per_second
        expr: rate(aicbc_api_requests_total[1m])
      - record: aicbc:llm_p95_seconds
        expr: histogram_quantile(0.95, rate(aicbc_llm_latency_seconds_bucket[5m]))
      - record: aicbc:llm_cache_hit_ratio
        expr: |
          (
            aicbc_llm_cache_hits_total
            /
            (aicbc_llm_cache_hits_total + aicbc_llm_cache_misses_total)
          )
```

- [ ] **Step 2: 修改 prometheus.yml 挂载 rules**

将 `docker/prometheus.yml` 中的 `rule_files` 取消注释并更新为：

```yaml
rule_files:
  - /etc/prometheus/rules/*.yml
```

并在 `scrape_configs` 上方添加 alertmanager 配置：

```yaml
alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']
```

- [ ] **Step 3: 在 docker-compose.yml 中挂载 rules**

在 `prometheus` 服务的 `volumes` 中追加：

```yaml
- ./docker/prometheus-rules.yml:/etc/prometheus/rules/aicbc.yml:ro
```

- [ ] **Step 4: Commit**

```bash
git add docker/prometheus-rules.yml docker/prometheus.yml docker-compose.yml
git commit -m "perf(monitoring): add Prometheus recording rules for performance SLOs"
```

---

### Task 6: 创建 Alertmanager 配置与告警规则

**Files:**
- Create: `docker/alertmanager.yml`
- Modify: `docker-compose.yml`

- [ ] **Step 1: 编写 alertmanager.yml**

```yaml
global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'severity']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: 'default'
  routes:
    - match:
        severity: critical
      receiver: 'default'
      continue: true

receivers:
  - name: 'default'
    webhook_configs:
      - url: 'http://api:8000/api/v1/admin/alerts'
        send_resolved: true
inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname']
```

- [ ] **Step 2: 将告警规则合并到 prometheus-rules.yml**

在 `docker/prometheus-rules.yml` 中追加第二个 group：

```yaml
  - name: aicbc_alerts
    rules:
      - alert: AicbcHighP95Latency
        expr: aicbc:api_request_p95_seconds > 2
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "AI_CBC API P95 latency is high"
          description: "P95 latency is {{ $value }}s for endpoint {{ $labels.endpoint }}"

      - alert: AicbcHighP99Latency
        expr: aicbc:api_request_p99_seconds > 5
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "AI_CBC API P99 latency is very high"

      - alert: AicbcHighErrorRate
        expr: |
          (
            sum(rate(aicbc_api_requests_total{status=~"5.."}[2m]))
            /
            sum(rate(aicbc_api_requests_total[2m]))
          ) > 0.01
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "AI_CBC API 5xx error rate is above 1%"

      - alert: AicbcWorkerOOMRisk
        expr: |
          (
            process_resident_memory_bytes{job="aicbc-api"}
            /
            container_spec_memory_limit_bytes{job="aicbc-api"}
          ) > 0.85
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "AI_CBC worker memory usage is above 85%"

      - alert: AicbcCeleryQueueBacklog
        expr: aicbc_queue_size{queue_name="analysis"} > 10
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "Celery analysis queue backlog is above 10"

      - alert: AicbcLLMHighLatency
        expr: aicbc:llm_p95_seconds > 30
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "LLM P95 latency is above 30s"
```

- [ ] **Step 3: 在 docker-compose.yml 中添加 alertmanager 服务**

在 `prometheus` 服务之前插入：

```yaml
  alertmanager:
    image: prom/alertmanager:v0.27
    container_name: aicbc-alertmanager
    ports:
      - "9093:9093"
    volumes:
      - ./docker/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
    restart: unless-stopped
    networks:
      - aicbc-network
```

- [ ] **Step 4: Commit**

```bash
git add docker/alertmanager.yml docker/prometheus-rules.yml docker-compose.yml
git commit -m "perf(monitoring): add Alertmanager config and performance alerts"
```

---

### Task 7: 更新 Grafana Dashboard

**Files:**
- Modify: `docker/grafana/dashboards/aicbc-dashboard.json`

- [ ] **Step 1: 添加新面板到 dashboard JSON**

由于 dashboard JSON 通常很大，这里给出追加面板的策略：在现有 dashboard 的 `panels` 数组末尾追加以下 4 个面板定义（注意 `id` 需唯一）：

```json
{
  "id": 50,
  "title": "API RPS / Errors / P95",
  "type": "timeseries",
  "targets": [
    {
      "expr": "sum(rate(aicbc_api_requests_total[1m]))",
      "legendFormat": "RPS"
    },
    {
      "expr": "sum(rate(aicbc_api_requests_total{status=~\"5..\"}[1m]))",
      "legendFormat": "5xx/s"
    },
    {
      "expr": "aicbc:api_request_p95_seconds",
      "legendFormat": "P95"
    }
  ],
  "gridPos": {"h": 8, "w": 12, "x": 0, "y": 40}
},
{
  "id": 51,
  "title": "LLM Calls / P95 Latency / Cache Hit Ratio",
  "type": "timeseries",
  "targets": [
    {
      "expr": "sum(rate(aicbc_llm_api_calls_total[1m]))",
      "legendFormat": "calls/s"
    },
    {
      "expr": "aicbc:llm_p95_seconds",
      "legendFormat": "P95 latency"
    },
    {
      "expr": "aicbc_cache_hit_ratio",
      "legendFormat": "cache hit ratio"
    }
  ],
  "gridPos": {"h": 8, "w": 12, "x": 12, "y": 40}
},
{
  "id": 52,
  "title": "Celery Queue Depth / Task Duration",
  "type": "timeseries",
  "targets": [
    {
      "expr": "aicbc_queue_size",
      "legendFormat": "{{queue_name}}"
    },
    {
      "expr": "histogram_quantile(0.95, rate(aicbc_persona_generation_duration_seconds_bucket[5m]))",
      "legendFormat": "persona gen P95"
    }
  ],
  "gridPos": {"h": 8, "w": 12, "x": 0, "y": 48}
},
{
  "id": 53,
  "title": "MongoDB Query Duration P95",
  "type": "timeseries",
  "targets": [
    {
      "expr": "histogram_quantile(0.95, rate(aicbc_mongodb_query_duration_seconds_bucket[5m]))",
      "legendFormat": "{{collection}}/{{operation}}"
    }
  ],
  "gridPos": {"h": 8, "w": 12, "x": 12, "y": 48}
}
```

- [ ] **Step 2: 验证 JSON 语法**

Run: `python -c "import json; json.load(open('docker/grafana/dashboards/aicbc-dashboard.json'))"`
Expected: 无输出。

- [ ] **Step 3: Commit**

```bash
git add docker/grafana/dashboards/aicbc-dashboard.json
git commit -m "perf(monitoring): extend Grafana dashboard with performance panels"
```

---

## Phase 2: 代码优化

### Task 8: 在 Settings 中新增环境变量

**Files:**
- Modify: `src/aicbc/config/settings.py`

- [ ] **Step 1: 扩展 DatabaseSettings**

```python
class DatabaseSettings(BaseSettings):
    """Database configuration."""

    mongodb_url: str = Field(default="mongodb://localhost:27017", alias="MONGODB_URL")
    mongodb_database: str = Field(default="aicbc", alias="MONGODB_DATABASE")
    mongodb_max_connections: int = Field(default=50, alias="MONGODB_MAX_CONNECTIONS")
    mongodb_min_connections: int = Field(default=10, alias="MONGODB_MIN_CONNECTIONS")
    mongodb_max_idle_time_ms: int = Field(default=60000, alias="MONGODB_MAX_IDLE_TIME_MS")
    mongodb_wait_queue_timeout_ms: int = Field(default=5000, alias="MONGODB_WAIT_QUEUE_TIMEOUT_MS")
    mongodb_server_selection_timeout_ms: int = Field(default=5000, alias="MONGODB_SERVER_SELECTION_TIMEOUT_MS")
    redis_url: str = Field(default="redis://localhost:6379/0", alias="REDIS_URL")
    redis_password: str = Field(default="", alias="REDIS_PASSWORD")
```

- [ ] **Step 2: 扩展 LLMSettings**

```python
class LLMSettings(BaseSettings):
    """LLM API configuration."""

    model_config = SettingsConfigDict(env_prefix="LLM_")

    provider: str = "anthropic"
    model: str = ""
    temperature: float = 0.3
    max_tokens: int = 4096
    timeout_seconds: int = 120
    max_retries: int = 3
    http_max_connections: int = Field(default=20, alias="HTTP_MAX_CONNECTIONS")
    http_max_keepalive: int = Field(default=10, alias="HTTP_MAX_KEEPALIVE")
```

- [ ] **Step 3: 扩展 Settings**

在 `Settings` 类中替换以下字段：

```python
    # Monitoring
    metrics_path: str = "/metrics"
    slow_request_threshold: float = 2.0

    # Caching
    use_redis_cache: bool = Field(default=False, alias="USE_REDIS_CACHE")
    dashboard_cache_ttl: int = Field(default=30, alias="DASHBOARD_CACHE_TTL")

    # Graceful shutdown
    api_graceful_shutdown_timeout: int = Field(default=30, alias="API_GRACEFUL_SHUTDOWN_TIMEOUT")
```

- [ ] **Step 4: 运行设置相关测试**

Run: `uv run pytest tests/test_settings.py -v`（如存在）或 `uv run python -c "from aicbc.config.settings import get_settings; print(get_settings())"`
Expected: 无异常。

- [ ] **Step 5: Commit**

```bash
git add src/aicbc/config/settings.py
git commit -m "perf(config): add performance-related environment variables"
```

---

### Task 9: 更新 .env.example

**Files:**
- Modify: `.env.example`

- [ ] **Step 1: 在 MongoDB 区追加**

```bash
MONGODB_MIN_CONNECTIONS=10
MONGODB_MAX_IDLE_TIME_MS=60000
MONGODB_WAIT_QUEUE_TIMEOUT_MS=5000
MONGODB_SERVER_SELECTION_TIMEOUT_MS=5000
```

- [ ] **Step 2: 在 LLM 调用参数区追加**

```bash
LLM_HTTP_MAX_CONNECTIONS=20
LLM_HTTP_MAX_KEEPALIVE=10
```

- [ ] **Step 3: 在监控配置区追加**

```bash
USE_REDIS_CACHE=false
DASHBOARD_CACHE_TTL=30
API_GRACEFUL_SHUTDOWN_TIMEOUT=30
SLOW_REQUEST_THRESHOLD=2.0
```

- [ ] **Step 4: Commit**

```bash
git add .env.example
git commit -m "docs(env): document new performance environment variables"
```

---

### Task 10: MongoDB 研究聚合与投影方法

**Files:**
- Modify: `src/aicbc/core/store_mongo.py`
- Test: `tests/test_store_mongo.py`

- [ ] **Step 1: 在 MongoQuestionnaireStore 中添加聚合方法**

在 `MongoQuestionnaireStore` 类中 `alist_studies` 之后添加：

```python
    async def acount_studies_by_status(self) -> dict[str, int]:
        """Return study counts grouped by status using MongoDB aggregation."""
        pipeline: list[dict[str, Any]] = [
            {"$group": {"_id": "$status", "count": {"$sum": 1}}},
        ]
        cursor = StudyDocument.get_motor_collection().aggregate(pipeline)
        results = await cursor.to_list(length=None)
        return {str(r["_id"]): r["count"] for r in results}

    async def alist_recent_studies(
        self,
        since: datetime,
        limit: int = 10,
    ) -> tuple[list[dict[str, Any]], int]:
        """Return lightweight recent study summaries and total count.

        Uses projection to avoid transferring the full ``data`` field.
        """
        query: Any = {"created_at": {"$gte": since}}
        projection = {
            "study_id": 1,
            "product_category": 1,
            "status": 1,
            "created_at": 1,
            "_id": 0,
        }
        total = await StudyDocument.find(query).count()
        docs = (
            await StudyDocument.find(query, projection=projection)
            .sort(-StudyDocument.created_at)
            .limit(limit)
            .to_list()
        )
        return docs, total
```

- [ ] **Step 2: 添加同步包装方法**

在同一类中添加：

```python
    def count_studies_by_status(self) -> dict[str, int]:
        """Synchronous version of :meth:`acount_studies_by_status`."""
        return _run_coro(self.acount_studies_by_status())

    def list_recent_studies(
        self,
        since: datetime,
        limit: int = 10,
    ) -> tuple[list[dict[str, Any]], int]:
        """Synchronous version of :meth:`alist_recent_studies`."""
        return _run_coro(self.alist_recent_studies(since, limit))
```

- [ ] **Step 3: 编写测试**

```python
"""Tests for MongoDB store optimizations."""

from datetime import UTC, datetime, timedelta

import pytest

from aicbc.core.store_mongo import MongoQuestionnaireStore


@pytest.mark.asyncio
async def test_acount_studies_by_status(memory_study_store):
    store = MongoQuestionnaireStore()
    # memory_study_store fixture should already contain some studies
    counts = await store.acount_studies_by_status()
    assert isinstance(counts, dict)


@pytest.mark.asyncio
async def test_alist_recent_studies_projection(memory_study_store):
    store = MongoQuestionnaireStore()
    since = datetime.now(UTC) - timedelta(days=30)
    docs, total = await store.alist_recent_studies(since, limit=10)
    for doc in docs:
        assert "data" not in doc
        assert "study_id" in doc
        assert "status" in doc
```

- [ ] **Step 4: 运行测试**

Run: `uv run pytest tests/test_store_mongo.py -v`
Expected: 通过（如 fixture 不存在，请按项目现有 fixture 命名调整测试）。

- [ ] **Step 5: Commit**

```bash
git add src/aicbc/core/store_mongo.py tests/test_store_mongo.py
git commit -m "perf(store): add MongoDB aggregation and projection helpers for studies"
```

---

### Task 11: 优化 Dashboard Summary 端点

**Files:**
- Modify: `src/aicbc/monitoring/health.py`
- Modify: `src/aicbc/core/cache.py`
- Test: `tests/test_dashboard_summary.py`

- [ ] **Step 1: 修改 cache TTL**

在 `src/aicbc/core/cache.py` 中：

```python
# Process-local caches for high-traffic read endpoints.
_dashboard_summary_cache = TimedCache(ttl_seconds=30)
_studies_list_cache = TimedCache(ttl_seconds=30)
_personas_list_cache = TimedCache(ttl_seconds=30)
```

- [ ] **Step 2: 修改 dashboard_summary 函数**

替换 `/dashboard/summary` 路由实现为：

```python
@router.get(
    "/dashboard/summary",
    summary="Aggregated dashboard statistics",
    tags=["Health"],
)
async def dashboard_summary() -> dict[str, Any]:
    """Aggregated dashboard statistics from all subsystems.

    Uses MongoDB aggregation for status counts and projection for recent
    studies. Cached for 30s to reduce load on frequently-refreshed dashboard.
    """
    from datetime import UTC, datetime, timedelta

    from aicbc.monitoring.metrics import record_mongodb_query_duration

    cache = get_dashboard_summary_cache()
    cached = cache.get("summary")
    if cached is not None:
        return cached  # type: ignore[return-value]

    persona_store = get_store()
    study_store = get_questionnaire_store()

    start = datetime.now(UTC)
    study_status_counts = await study_store.acount_studies_by_status()
    record_mongodb_query_duration("studies", "aggregate_count", (datetime.now(UTC) - start).total_seconds())

    total_studies = sum(study_status_counts.values())

    start = datetime.now(UTC)
    persona_count = await persona_store.acount()
    record_mongodb_query_duration("personas", "count", (datetime.now(UTC) - start).total_seconds())

    week_ago = datetime.now(UTC) - timedelta(days=7)
    start = datetime.now(UTC)
    recent_docs, _ = await study_store.alist_recent_studies(week_ago, limit=10)
    record_mongodb_query_duration("studies", "find_projection", (datetime.now(UTC) - start).total_seconds())

    recent_studies = [
        {
            "study_id": doc["study_id"],
            "product_category": doc.get("product_category", ""),
            "status": doc["status"],
            "created_at": doc["created_at"].isoformat(),
        }
        for doc in recent_docs
    ]

    result = {
        "summary": {
            "total_studies": total_studies,
            "total_personas": persona_count,
            "studies_by_status": study_status_counts,
            "recent_studies_last_7d": len(recent_studies),
        },
        "recent_studies": recent_studies,
    }
    cache.set("summary", result)
    return result
```

- [ ] **Step 3: 编写测试**

```python
"""Tests for dashboard summary optimization."""

from unittest.mock import patch

from aicbc.monitoring.health import dashboard_summary


@pytest.mark.asyncio
async def test_dashboard_summary_uses_cache():
    first = await dashboard_summary()
    second = await dashboard_summary()
    assert first == second


@pytest.mark.asyncio
async def test_dashboard_summary_invalidates():
    from aicbc.core.cache import invalidate_dashboard_summary

    first = await dashboard_summary()
    invalidate_dashboard_summary()
    with patch("aicbc.core.store_mongo.MongoQuestionnaireStore.acount_studies_by_status") as mock_count:
        mock_count.return_value = {"draft": 5}
        second = await dashboard_summary()
    assert first != second or second["summary"]["studies_by_status"].get("draft") == 5
```

- [ ] **Step 4: 运行测试**

Run: `uv run pytest tests/test_dashboard_summary.py -v`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/aicbc/monitoring/health.py src/aicbc/core/cache.py tests/test_dashboard_summary.py
git commit -m "perf(health): optimize dashboard summary with MongoDB aggregation and projection"
```

---

### Task 12: 新增画像生成任务文档模型

**Files:**
- Modify: `src/aicbc/core/models/db_documents.py`
- Test: `tests/test_db_documents.py`

- [ ] **Step 1: 添加 PersonaGenerationJobDocument**

在 `DeadLetterDocument` 之前插入：

```python
class PersonaGenerationJobDocument(Document):
    """Persisted state for async persona generation Celery tasks."""

    job_id: Indexed(str, unique=True)
    study_id: Indexed(str)
    status: Indexed(str)
    requested: int
    generated: int = 0
    failed: int = 0
    total_cost_cny: float = 0.0
    bias_failed_count: int = 0
    bias_warning: str | None = None
    data: dict[str, Any] = Field(default_factory=dict)
    created_at: datetime = Field(default_factory=lambda: datetime.now(UTC))
    updated_at: datetime = Field(default_factory=lambda: datetime.now(UTC))

    class Settings:
        name = "persona_generation_jobs"
        indexes = [
            IndexModel([("created_at", -1)]),
            IndexModel([("status", 1)]),
        ]
```

- [ ] **Step 2: 更新 ALL_DOCUMENT_MODELS**

将 `PersonaGenerationJobDocument` 加入列表：

```python
ALL_DOCUMENT_MODELS: list[type[Document]] = [
    PersonaDocument,
    StudyDocument,
    QuestionnaireDocument,
    ResponseDocument,
    DatasetDocument,
    AnalysisJobDocument,
    AnalysisResultDocument,
    AnalysisDerivativeDocument,
    SettingsDocument,
    FeatureFlagDocument,
    AuditLogDocument,
    PersonaGenerationJobDocument,
    DeadLetterDocument,
]
```

- [ ] **Step 3: 运行模型验证**

Run: `uv run python -c "from aicbc.core.models.db_documents import ALL_DOCUMENT_MODELS; print([m.__name__ for m in ALL_DOCUMENT_MODELS])"`
Expected: 列表包含 PersonaGenerationJobDocument。

- [ ] **Step 4: Commit**

```bash
git add src/aicbc/core/models/db_documents.py
git commit -m "perf(db): add PersonaGenerationJobDocument for async batch generation"
```

---

### Task 13: 重构 ConsumerGeneratorAgent 支持 Celery Loop

**Files:**
- Modify: `src/aicbc/agents/consumer_generator.py`

- [ ] **Step 1: 抽离 async batch core**

将 `generate_batch` 中的内部 `_batch_core` 提升为实例方法，并新增 `run_batch_core` 方法：

```python
    async def _batch_core(
        self,
        study_id: str,
        count: int,
        life_stages: list[str] | None,
        seed: int | None,
        max_concurrency: int,
    ) -> list:
        semaphore = asyncio.Semaphore(max_concurrency)

        async def _bounded_generate(index: int):
            async with semaphore:
                return index, await asyncio.to_thread(
                    self.generate_single,
                    study_id=study_id,
                    index=index,
                    life_stage=life_stages[index % len(life_stages)] if life_stages else None,
                    seed=(seed + index) if seed is not None else None,
                )

        tasks = [_bounded_generate(i) for i in range(count)]
        return list(await asyncio.gather(*tasks, return_exceptions=True))

    def generate_batch(
        self,
        study_id: str,
        count: int,
        life_stages: list[str] | None = None,
        seed: int | None = None,
        max_concurrency: int = 3,
    ) -> tuple[list[PersonaProfile], list[AgentState], dict[str, Any]]:
        """Generate a batch of personas concurrently with error isolation.

        Standalone entry point that creates its own event loop.
        """
        raw_results = asyncio.run(self._batch_core(study_id, count, life_stages, seed, max_concurrency))
        return self._assemble_batch_results(raw_results)

    def generate_batch_on_loop(
        self,
        loop: asyncio.AbstractEventLoop,
        study_id: str,
        count: int,
        life_stages: list[str] | None = None,
        seed: int | None = None,
        max_concurrency: int = 3,
    ) -> tuple[list[PersonaProfile], list[AgentState], dict[str, Any]]:
        """Generate a batch using an existing event loop (Celery worker)."""
        raw_results = loop.run_until_complete(
            self._batch_core(study_id, count, life_stages, seed, max_concurrency)
        )
        return self._assemble_batch_results(raw_results)

    def _assemble_batch_results(
        self,
        raw_results: list,
    ) -> tuple[list[PersonaProfile], list[AgentState], dict[str, Any]]:
        """Reassemble results from _batch_core into profiles/states/summary."""
        profiles: list[PersonaProfile] = []
        states: list[AgentState] = []
        failures: list[dict[str, Any]] = []
        total_corrections = 0
        passed_count = 0

        indexed: list[tuple[int, Any]] = []
        for item in raw_results:
            if isinstance(item, Exception):
                self._log.error(
                    "batch_gather_unexpected_failure",
                    error_type=type(item).__name__,
                    error=str(item),
                )
                continue
            indexed.append(item)
        indexed.sort(key=lambda x: x[0])

        for index, payload in indexed:
            if isinstance(payload, Exception):
                self._log.error(
                    "batch_persona_failed",
                    index=index,
                    error_type=type(payload).__name__,
                    error=str(payload),
                )
                failures.append({"index": index, "error": str(payload)[:200]})
                continue

            profile, state = payload
            profiles.append(profile)
            states.append(state)
            total_corrections += state.correction_count
            if (
                profile.authenticity_score is not None
                and profile.authenticity_score >= self._threshold
            ):
                passed_count += 1

        generated_count = len(profiles)
        failed_count = len(failures)

        summary = {
            "requested": generated_count + failed_count,
            "generated": generated_count,
            "failed": failed_count,
            "passed_authenticity": passed_count,
            "failed_authenticity": generated_count - passed_count,
            "total_corrections": total_corrections,
            "avg_authenticity_score": sum(p.authenticity_score or 0 for p in profiles)
            / max(generated_count, 1),
            "concurrency": max(1, max_concurrency),
            "failures": failures,
        }

        self._log.info("batch_generation_complete", **summary)
        return profiles, states, summary
```

- [ ] **Step 2: 删除原 generate_batch 中重复的结果组装代码**

将原 `generate_batch` 的函数体替换为对新方法的调用。

- [ ] **Step 3: 验证语法**

Run: `uv run python -m py_compile src/aicbc/agents/consumer_generator.py`
Expected: 无输出。

- [ ] **Step 4: Commit**

```bash
git add src/aicbc/agents/consumer_generator.py
git commit -m "perf(agents): refactor batch generation for Celery worker loop reuse"
```

---

### Task 14: 新增 Celery 画像生成任务

**Files:**
- Modify: `src/aicbc/analysis/tasks.py`
- Test: `tests/test_persona_generation_task.py`

- [ ] **Step 1: 在 tasks.py 末尾追加任务**

```python
@celery_app.task(
    bind=True,
    name="aicbc.analysis.run_persona_generation_task",
    time_limit=1800,  # 30 minutes for large batches
    soft_time_limit=1500,
)
def run_persona_generation_task(
    self,
    job_id: str,
    request_json: str,
) -> dict:
    """Run persona batch generation as a background Celery task."""
    from datetime import UTC, datetime

    from aicbc.agents.consumer_generator import ConsumerGeneratorAgent
    from aicbc.core.models.db_documents import PersonaGenerationJobDocument
    from aicbc.core.store import get_store
    from aicbc.monitoring.metrics import record_persona_generation_task

    request = json.loads(request_json)
    study_id = request["study_id"]
    count = request["count"]
    seed = request.get("seed")
    life_stages = request.get("life_stages")

    log = logger.bind(job_id=job_id, study_id=study_id, count=count)
    log.info("persona_generation_task_started")

    async def _update_status(status: str, progress: float, **fields) -> None:
        doc = await PersonaGenerationJobDocument.find_one(
            PersonaGenerationJobDocument.job_id == job_id
        )
        if doc is None:
            return
        doc.status = status
        doc.progress = progress
        for key, value in fields.items():
            setattr(doc, key, value)
        doc.updated_at = datetime.now(UTC)
        await doc.save()

    _run_async(_update_status("RUNNING", 0.0))

    start_time = time.perf_counter()
    try:
        agent = ConsumerGeneratorAgent()
        loop = _get_worker_loop()
        profiles, states, summary = agent.generate_batch_on_loop(
            loop=loop,
            study_id=study_id,
            count=count,
            life_stages=life_stages,
            seed=seed,
            max_concurrency=3,
        )

        store = get_store()
        saved = 0
        for profile in profiles:
            # acsave expects async store; get_store returns sync wrapper in worker context
            if store.save(profile):
                saved += 1

        total_cost = sum(p.generation_metadata.cost_cny for p in profiles)
        elapsed = time.perf_counter() - start_time

        _run_async(
            _update_status(
                "COMPLETED",
                100.0,
                generated=saved,
                failed=summary["failed"],
                total_cost_cny=round(total_cost, 4),
                data={"summary": summary, "states": [s.model_dump(mode="json") for s in states]},
            )
        )
        record_persona_generation_task(study_id, elapsed, count)
        log.info("persona_generation_task_completed", generated=saved, elapsed=elapsed)
        return {"status": "COMPLETED", "job_id": job_id, "generated": saved}

    except Exception as exc:
        elapsed = time.perf_counter() - start_time
        log.exception("persona_generation_task_failed")
        _run_async(
            _update_status(
                "FAILED",
                0.0,
                data={"error": f"{type(exc).__name__}: {exc}"},
            )
        )
        record_persona_generation_task(study_id, elapsed, count)
        raise
```

- [ ] **Step 2: 在 Celery 路由中添加队列**

在 `celery_app.conf.update` 的 `task_routes` 中追加：

```python
    task_routes={
        "aicbc.analysis.*": {"queue": "analysis"},
        "aicbc.analysis.run_persona_generation_task": {"queue": "persona_generation"},
    },
```

- [ ] **Step 3: 编写测试**

```python
"""Tests for async persona generation Celery task."""

import json

import pytest

from aicbc.analysis.tasks import run_persona_generation_task


def test_persona_generation_task_signature() -> None:
    assert run_persona_generation_task.name == "aicbc.analysis.run_persona_generation_task"


@pytest.mark.slow
@pytest.mark.skipif(not os.getenv("CI"), reason="Requires running Celery worker")
def test_persona_generation_task_runs() -> None:
    request = {
        "study_id": "test-async-gen",
        "count": 2,
        "seed": 42,
        "life_stages": ["白领"],
    }
    result = run_persona_generation_task.delay("job-test-1", json.dumps(request))
    assert result.get(timeout=120)["status"] == "COMPLETED"
```

- [ ] **Step 4: 运行测试**

Run: `uv run pytest tests/test_persona_generation_task.py -v -m "not slow"`
Expected: signature 测试通过，slow 测试被跳过。

- [ ] **Step 5: Commit**

```bash
git add src/aicbc/analysis/tasks.py tests/test_persona_generation_task.py
git commit -m "perf(tasks): add Celery task for async persona batch generation"
```

---

### Task 15: 新增异步画像生成 API 与状态轮询

**Files:**
- Modify: `src/aicbc/api/routes/personas.py`
- Modify: `src/aicbc/api/schemas.py`
- Test: `tests/test_persona_generation_api.py`

- [ ] **Step 1: 扩展 API schemas**

在 `src/aicbc/api/schemas.py` 中追加：

```python
class AsyncBatchGenerateResponse(BaseModel):
    """Response for async persona generation request."""

    job_id: str
    study_id: str
    requested: int
    status: str
    message: str


class PersonaGenerationJobStatusResponse(BaseModel):
    """Status of an async persona generation job."""

    job_id: str
    study_id: str
    status: str
    requested: int
    generated: int
    failed: int
    total_cost_cny: float
    progress: float
    bias_failed_count: int = 0
    bias_warning: str | None = None
    created_at: datetime
    updated_at: datetime
```

- [ ] **Step 2: 在 personas.py 中添加新路由**

在 `generate_personas_batch` 函数之后添加：

```python
import uuid

from aicbc.analysis.tasks import run_persona_generation_task
from aicbc.core.models.db_documents import PersonaGenerationJobDocument


@router.post(
    "/personas/generate-async",
    response_model=AsyncBatchGenerateResponse,
    status_code=status.HTTP_202_ACCEPTED,
    summary="Generate a batch of personas asynchronously",
)
async def generate_personas_async(
    request: BatchGenerateRequest,
) -> AsyncBatchGenerateResponse:
    """Enqueue a large persona generation batch and return immediately."""
    safe_study_id = sanitize_id(request.study_id, field_name="study_id")
    job_id = f"pg-{safe_study_id}-{uuid.uuid4().hex[:8]}"

    job = PersonaGenerationJobDocument(
        job_id=job_id,
        study_id=safe_study_id,
        status="QUEUED",
        requested=request.count,
    )
    await job.insert()

    payload = {
        "study_id": safe_study_id,
        "count": request.count,
        "seed": request.seed,
    }
    run_persona_generation_task.delay(job_id, json.dumps(payload))

    return AsyncBatchGenerateResponse(
        job_id=job_id,
        study_id=safe_study_id,
        requested=request.count,
        status="QUEUED",
        message="Persona generation queued; poll /personas/generation/{job_id}/status",
    )


@router.get(
    "/personas/generation/{job_id}/status",
    response_model=PersonaGenerationJobStatusResponse,
    summary="Get async persona generation job status",
)
async def get_persona_generation_status(
    job_id: str,
) -> PersonaGenerationJobStatusResponse:
    """Poll the status of an async persona generation job."""
    doc = await PersonaGenerationJobDocument.find_one(
        PersonaGenerationJobDocument.job_id == job_id
    )
    if doc is None:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Generation job '{job_id}' not found",
        )
    return PersonaGenerationJobStatusResponse(
        job_id=doc.job_id,
        study_id=doc.study_id,
        status=doc.status,
        requested=doc.requested,
        generated=doc.generated,
        failed=doc.failed,
        total_cost_cny=doc.total_cost_cny,
        progress=getattr(doc, "progress", 0.0),
        bias_failed_count=doc.bias_failed_count,
        bias_warning=doc.bias_warning,
        created_at=doc.created_at,
        updated_at=doc.updated_at,
    )
```

- [ ] **Step 3: 修改原同步端点保留小批量快速路径**

在 `generate_personas_batch` 开头（参数校验后）添加：

```python
    # Fast path: small batches run synchronously to avoid task overhead.
    if request.count <= 5:
        log.info("batch_fast_path", count=request.count)
        # ... existing full logic continues below unchanged
```

- [ ] **Step 4: 编写测试**

```python
"""Tests for async persona generation API."""

import pytest
from fastapi import status
from httpx import AsyncClient


@pytest.mark.asyncio
async def test_generate_personas_async_returns_202(client: AsyncClient):
    response = await client.post("/api/v1/personas/generate-async", json={
        "study_id": "async-test",
        "count": 10,
    })
    assert response.status_code == status.HTTP_202_ACCEPTED
    data = response.json()
    assert data["status"] == "QUEUED"
    assert "job_id" in data


@pytest.mark.asyncio
async def test_get_persona_generation_status_404(client: AsyncClient):
    response = await client.get("/api/v1/personas/generation/nonexistent/status")
    assert response.status_code == status.HTTP_404_NOT_FOUND
```

- [ ] **Step 5: 运行测试**

Run: `uv run pytest tests/test_persona_generation_api.py -v`
Expected: 通过。

- [ ] **Step 6: Commit**

```bash
git add src/aicbc/api/routes/personas.py src/aicbc/api/schemas.py tests/test_persona_generation_api.py
git commit -m "perf(api): add async persona generation endpoint with status polling"
```

---

### Task 16: LLM 客户端异步化

**Files:**
- Modify: `src/aicbc/llm/client.py`
- Test: `tests/test_llm_async_client.py`

- [ ] **Step 1: 添加 AsyncClient 与异步调用方法**

在 `LLMClient.__init__` 中追加：

```python
        self._async_http_client: httpx.AsyncClient | None = None
```

新增属性与方法：

```python
    def _get_async_client(self) -> httpx.AsyncClient:
        """Return a lazily initialized async HTTP client."""
        if self._async_http_client is None or self._async_http_client.is_closed:
            settings = get_settings()
            self._async_http_client = httpx.AsyncClient(
                limits=httpx.Limits(
                    max_connections=settings.llm.http_max_connections,
                    max_keepalive_connections=settings.llm.http_max_keepalive,
                ),
                timeout=httpx.Timeout(settings.llm.timeout_seconds),
            )
        return self._async_http_client

    async def aclose(self) -> None:
        """Close the async HTTP client if initialized."""
        if self._async_http_client is not None and not self._async_http_client.is_closed:
            await self._async_http_client.aclose()
```

- [ ] **Step 2: 新增异步 Anthropic/OpenAI 调用方法**

在 `_call_anthropic` 和 `_call_openai` 之后分别添加异步版本：

```python
    async def _call_anthropic_async(
        self,
        client: Anthropic,
        model: str,
        messages: list[dict[str, str]],
        temperature: float,
        max_tokens: int,
        json_mode: bool,
    ) -> LLMResponse:
        """Async Anthropic API call using the SDK's async client path."""
        from anthropic import AsyncAnthropic

        settings = get_settings()
        async_client = AsyncAnthropic(
            api_key=settings.anthropic.api_key,
            base_url=settings.anthropic.base_url,
            timeout=settings.llm.timeout_seconds,
            http_client=self._get_async_client(),
        )
        return await asyncio.to_thread(
            self._call_anthropic,
            client=client,
            model=model,
            messages=messages,
            temperature=temperature,
            max_tokens=max_tokens,
            json_mode=json_mode,
        )

    async def _call_openai_async(
        self,
        client: OpenAI,
        model: str,
        messages: list[dict[str, str]],
        temperature: float,
        max_tokens: int,
        json_mode: bool,
    ) -> LLMResponse:
        """Async OpenAI-compatible API call."""
        return await asyncio.to_thread(
            self._call_openai,
            client=client,
            model=model,
            messages=messages,
            temperature=temperature,
            max_tokens=max_tokens,
            json_mode=json_mode,
        )
```

- [ ] **Step 3: 修改 generate_async 使用异步路径**

将 `generate_async` 内部的调用替换为：

```python
                if resolved_provider == Provider.ANTHROPIC:
                    result = await self._call_anthropic_async(
                        client=client,  # type: ignore[arg-type]
                        model=resolved_model,
                        messages=messages,
                        temperature=resolved_temperature,
                        max_tokens=resolved_max_tokens,
                        json_mode=json_mode,
                    )
                else:
                    result = await self._call_openai_async(
                        client=client,  # type: ignore[arg-type]
                        model=resolved_model,
                        messages=messages,
                        temperature=resolved_temperature,
                        max_tokens=resolved_max_tokens,
                        json_mode=json_mode,
                    )
```

- [ ] **Step 4: 编写测试**

```python
"""Tests for LLM async client path."""

import pytest

from aicbc.llm.client import LLMClient


@pytest.mark.asyncio
async def test_llm_client_has_async_client() -> None:
    client = LLMClient()
    async_client = client._get_async_client()
    assert isinstance(async_client, httpx.AsyncClient)
    await client.aclose()


@pytest.mark.asyncio
async def test_generate_async_does_not_block_event_loop() -> None:
    client = LLMClient()
    # Without a real API key this will raise, but it should fail on auth/network,
    # not on "no running event loop" or thread-pool exhaustion.
    with pytest.raises(Exception):
        await client.generate_async(messages=[{"role": "user", "content": "hello"}])
```

- [ ] **Step 5: 运行测试**

Run: `uv run pytest tests/test_llm_async_client.py -v`
Expected: `_get_async_client` 测试通过；`generate_async` 测试因无密钥失败但异常类型不是 RuntimeError。

- [ ] **Step 6: Commit**

```bash
git add src/aicbc/llm/client.py tests/test_llm_async_client.py
git commit -m "perf(llm): add httpx.AsyncClient path for generate_async"
```

---

### Task 17: MongoDB 查询投影与连接池

**Files:**
- Modify: `src/aicbc/core/store_mongo.py`
- Modify: `src/aicbc/main.py`
- Test: `tests/test_store_projection.py`

- [ ] **Step 1: 为列表方法添加投影支持**

在 `MongoPersonaStore.alist_all` 中，当调用方只需要摘要时，使用 projection。由于现有接口返回 `PersonaProfile`，新增 `alist_all_lightweight` 方法：

```python
    async def alist_all_lightweight(
        self,
        *,
        study_id: str | None = None,
        segment: str | None = None,
        city_tier: str | None = None,
        bias_status: str | None = None,
        page: int = 1,
        page_size: int = 20,
    ) -> tuple[list[dict[str, Any]], int]:
        """Return lightweight persona summaries without deserializing full data."""
        query: Any = {}
        if segment is not None:
            query["segment"] = segment
        if city_tier is not None:
            query["city"] = city_tier
        if bias_status is not None:
            query["bias_audit_status"] = bias_status
        if study_id is not None:
            prefix = f"persona-{study_id}-"
            query["persona_id"] = {"$regex": f"^{prefix}"}

        projection = {
            "persona_id": 1,
            "segment": 1,
            "city": 1,
            "bias_audit_status": 1,
            "status": 1,
            "created_at": 1,
            "_id": 0,
        }
        mongo_query = PersonaDocument.find(query, projection=projection)
        total = await mongo_query.count()
        start = (page - 1) * page_size
        docs = await mongo_query.skip(start).limit(page_size).to_list()
        return docs, total
```

在 `MongoAnalysisStore.alist_jobs_by_study` 中添加投影版本：

```python
    async def alist_jobs_by_study_lightweight(
        self,
        study_id: str,
    ) -> list[dict[str, Any]]:
        """Return lightweight analysis job summaries for a study."""
        projection = {
            "analysis_id": 1,
            "study_id": 1,
            "status": 1,
            "created_at": 1,
            "_id": 0,
        }
        docs = await AnalysisJobDocument.find(
            AnalysisJobDocument.study_id == study_id,
            projection=projection,
        ).to_list()
        return docs
```

- [ ] **Step 2: 在 main.py 中使用新连接池参数**

替换 MongoDB client 初始化：

```python
        _mongo_client = AsyncIOMotorClient(
            settings.database.mongodb_url,
            maxPoolSize=settings.database.mongodb_max_connections,
            minPoolSize=settings.database.mongodb_min_connections,
            maxIdleTimeMS=settings.database.mongodb_max_idle_time_ms,
            waitQueueTimeoutMS=settings.database.mongodb_wait_queue_timeout_ms,
            serverSelectionTimeoutMS=settings.database.mongodb_server_selection_timeout_ms,
        )
```

- [ ] **Step 3: 编写测试**

```python
"""Tests for MongoDB projection helpers."""

import pytest

from aicbc.core.store_mongo import MongoAnalysisStore, MongoPersonaStore


@pytest.mark.asyncio
async def test_alist_all_lightweight_excludes_data():
    store = MongoPersonaStore()
    docs, total = await store.alist_all_lightweight(page_size=5)
    assert total >= 0
    for doc in docs:
        assert "data" not in doc
        assert "persona_id" in doc


@pytest.mark.asyncio
async def test_alist_jobs_by_study_lightweight():
    store = MongoAnalysisStore()
    docs = await store.alist_jobs_by_study_lightweight("nonexistent-study")
    assert docs == []
```

- [ ] **Step 4: 运行测试**

Run: `uv run pytest tests/test_store_projection.py -v`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/aicbc/core/store_mongo.py src/aicbc/main.py tests/test_store_projection.py
git commit -m "perf(store): add MongoDB projection helpers and connection pool tuning"
```

---

### Task 18: 缓存策略增强（Redis 后端可选）

**Files:**
- Modify: `src/aicbc/core/cache.py`
- Test: `tests/test_cache_redis_backend.py`

- [ ] **Step 1: 重构 TimedCache 支持多后端**

```python
"""Pluggable TTL cache for single-worker or multi-worker deployments."""

from __future__ import annotations

import time
from typing import Any, Protocol

from aicbc.config.settings import get_settings


class CacheBackend(Protocol):
    """Cache backend protocol."""

    def get(self, key: Any) -> Any | None: ...
    def set(self, key: Any, value: Any, ttl_seconds: float) -> None: ...
    def clear(self) -> None: ...


class _MemoryBackend:
    """Process-local dict-based cache."""

    def __init__(self) -> None:
        self._store: dict[Any, tuple[Any, float]] = {}

    def get(self, key: Any) -> Any | None:
        value, expires = self._store.get(key, (None, 0.0))
        if time.monotonic() < expires:
            return value
        self._store.pop(key, None)
        return None

    def set(self, key: Any, value: Any, ttl_seconds: float) -> None:
        self._store[key] = (value, time.monotonic() + ttl_seconds)

    def clear(self) -> None:
        self._store.clear()


class _RedisBackend:
    """Redis-backed shared cache."""

    def __init__(self, redis_url: str) -> None:
        import redis

        self._client = redis.from_url(redis_url, decode_responses=True)

    def _encode(self, key: Any) -> str:
        return f"aicbc:cache:{hash(key)}"

    def get(self, key: Any) -> Any | None:
        import json

        raw = self._client.get(self._encode(key))
        if raw is None:
            return None
        return json.loads(raw)

    def set(self, key: Any, value: Any, ttl_seconds: float) -> None:
        import json

        self._client.setex(self._encode(key), int(ttl_seconds), json.dumps(value))

    def clear(self) -> None:
        for key in self._client.scan_iter(match="aicbc:cache:*"):
            self._client.delete(key)


class TimedCache:
    """Cache with pluggable backend."""

    def __init__(self, ttl_seconds: float, backend: CacheBackend | None = None) -> None:
        self.ttl = ttl_seconds
        self._backend = backend or _MemoryBackend()

    def _enabled(self) -> bool:
        env = get_settings().environment.lower()
        return env not in ("testing", "test") and self.ttl > 0

    def get(self, key: Any) -> Any | None:
        if not self._enabled():
            return None
        return self._backend.get(key)

    def set(self, key: Any, value: Any) -> None:
        if not self._enabled():
            return
        self._backend.set(key, value, self.ttl)

    def clear(self) -> None:
        self._backend.clear()


def _create_default_backend() -> CacheBackend:
    settings = get_settings()
    if settings.use_redis_cache:
        return _RedisBackend(settings.database.redis_url)
    return _MemoryBackend()


# Process-local caches for high-traffic read endpoints.
_dashboard_summary_cache = TimedCache(ttl_seconds=30, backend=_create_default_backend())
_studies_list_cache = TimedCache(ttl_seconds=30, backend=_create_default_backend())
_personas_list_cache = TimedCache(ttl_seconds=30, backend=_create_default_backend())
```

- [ ] **Step 2: 在 cache.py 中新增命中率跟踪**

在 `TimedCache` 中添加：

```python
        self._hits = 0
        self._misses = 0

    @property
    def hit_ratio(self) -> float:
        total = self._hits + self._misses
        return self._hits / total if total > 0 else 0.0

    def get(self, key: Any) -> Any | None:
        if not self._enabled():
            return None
        value = self._backend.get(key)
        if value is None:
            self._misses += 1
        else:
            self._hits += 1
        return value
```

- [ ] **Step 3: 编写测试**

```python
"""Tests for cache backend switching."""

import pytest

from aicbc.core.cache import TimedCache, _MemoryBackend, _RedisBackend


def test_memory_backend() -> None:
    cache = TimedCache(ttl_seconds=10, backend=_MemoryBackend())
    cache.set("k", "v")
    assert cache.get("k") == "v"
    cache.clear()
    assert cache.get("k") is None


def test_cache_hit_ratio() -> None:
    cache = TimedCache(ttl_seconds=10, backend=_MemoryBackend())
    cache.set("k", "v")
    cache.get("k")
    cache.get("missing")
    assert cache.hit_ratio == 0.5
```

- [ ] **Step 4: 运行测试**

Run: `uv run pytest tests/test_cache_redis_backend.py -v`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/aicbc/core/cache.py tests/test_cache_redis_backend.py
git commit -m "perf(cache): add optional Redis backend and hit-ratio tracking"
```

---

### Task 19: 轻量优化项（GZip、中间件顺序、慢请求阈值）

**Files:**
- Modify: `src/aicbc/main.py`
- Test: `tests/test_middleware_order.py`

- [ ] **Step 1: 调整中间件顺序与 GZip 级别**

在 `src/aicbc/main.py` 中：

```python
# Add middleware (order matters: metrics before rate limit so limited requests are counted)
app.add_middleware(MetricsMiddleware)
app.add_middleware(RateLimitMiddleware)
app.add_middleware(SecurityHeadersMiddleware)
app.add_middleware(AuthMiddleware)
app.add_middleware(RBACMiddleware)
app.add_middleware(AuditLogMiddleware)

# Compress JSON responses above 1 KB. Placed before CORS so CORS remains outermost.
app.add_middleware(GZipMiddleware, minimum_size=1000, compresslevel=6)
```

- [ ] **Step 2: 更新 docker-compose.yml 中 SLOW_REQUEST_THRESHOLD**

```yaml
      - SLOW_REQUEST_THRESHOLD=2.0
```

- [ ] **Step 3: 编写测试**

```python
"""Tests for middleware ordering."""

from fastapi import FastAPI

from aicbc.main import app


def test_metrics_middleware_before_rate_limit() -> None:
    # FastAPI stacks middleware in reverse order of registration.
    # The last registered is the outermost. We verify MetricsMiddleware
    # was registered before RateLimitMiddleware so it wraps it.
    assert app.user_middleware is not None
    names = [m.cls.__name__ for m in app.user_middleware]
    metrics_idx = names.index("MetricsMiddleware")
    rate_limit_idx = names.index("RateLimitMiddleware")
    assert metrics_idx < rate_limit_idx
```

- [ ] **Step 4: 运行测试**

Run: `uv run pytest tests/test_middleware_order.py -v`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/aicbc/main.py docker-compose.yml tests/test_middleware_order.py
git commit -m "perf(api): tune gzip level, reorder middleware, lower slow request threshold"
```

---

## Phase 3: 部署配置与优雅关闭

### Task 20: Dockerfile 多 Target 准备（不启用）

**Files:**
- Modify: `docker/Dockerfile`

- [ ] **Step 1: 在 builder stage 后添加 api target**

在现有 `runtime` stage 之后添加命名 target：

```dockerfile
# Stage 3: API target (default, backwards-compatible)
FROM runtime AS api
CMD ["/usr/local/bin/supervisord", "-c", "/etc/supervisor/conf.d/supervisord.conf"]

# Stage 4: Worker target — same image, but labelled for future split
FROM runtime AS worker
CMD ["celery", "-A", "src.aicbc.analysis.tasks", "worker", "--loglevel=info", "--concurrency=2", "-Q", "analysis,persona_generation"]
```

- [ ] **Step 2: 验证构建**

Run: `docker build -f docker/Dockerfile --target api -t aicbc:api-target .`
Expected: 构建成功。

- [ ] **Step 3: Commit**

```bash
git add docker/Dockerfile
git commit -m "build(docker): add api/worker build targets for future image split"
```

---

### Task 21: 资源限制补全

**Files:**
- Modify: `docker-compose.yml`
- Modify: `docker-compose.azure-b2ats.yml`
- Modify: `k8s/base/configmap.yaml`

- [ ] **Step 1: 补全 docker-compose.yml 资源限制**

为 `beat` 添加：

```yaml
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
```

为 `mongo` 添加 CPU 限制：

```yaml
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
```

为 `redis` 添加 CPU 限制：

```yaml
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
```

- [ ] **Step 2: 更新 azure-b2ats 新环境变量**

在 `docker-compose.azure-b2ats.yml` 的 `api.environment` 中追加：

```yaml
      - SLOW_REQUEST_THRESHOLD=2.0
      - DASHBOARD_CACHE_TTL=30
      - USE_REDIS_CACHE=false
      - LLM_HTTP_MAX_CONNECTIONS=20
      - LLM_HTTP_MAX_KEEPALIVE=10
      - MONGODB_MIN_CONNECTIONS=5
      - MONGODB_MAX_IDLE_TIME_MS=60000
      - MONGODB_WAIT_QUEUE_TIMEOUT_MS=5000
      - MONGODB_SERVER_SELECTION_TIMEOUT_MS=5000
      - API_GRACEFUL_SHUTDOWN_TIMEOUT=30
```

- [ ] **Step 3: 更新 k8s/base/configmap.yaml**

在 `data:` 中追加：

```yaml
  # Performance tuning
  SLOW_REQUEST_THRESHOLD: "2.0"
  DASHBOARD_CACHE_TTL: "30"
  USE_REDIS_CACHE: "false"
  LLM_HTTP_MAX_CONNECTIONS: "20"
  LLM_HTTP_MAX_KEEPALIVE: "10"
  MONGODB_MIN_CONNECTIONS: "10"
  MONGODB_MAX_IDLE_TIME_MS: "60000"
  MONGODB_WAIT_QUEUE_TIMEOUT_MS: "5000"
  MONGODB_SERVER_SELECTION_TIMEOUT_MS: "5000"
  API_GRACEFUL_SHUTDOWN_TIMEOUT: "30"
  # API_WORKERS can be tuned per Pod CPU limits; base default below
  API_WORKERS: "4"
```

- [ ] **Step 4: Commit**

```bash
git add docker-compose.yml docker-compose.azure-b2ats.yml k8s/base/configmap.yaml
git commit -m "perf(deploy): add resource limits and performance env vars to compose/k8s"
```

---

### Task 22: Graceful Shutdown

**Files:**
- Modify: `src/aicbc/main.py`
- Modify: `src/aicbc/api/middleware/rate_limit.py`
- Test: `tests/test_graceful_shutdown.py`

- [ ] **Step 1: 在 lifespan 中注册 SIGTERM 处理**

修改 `lifespan`：

```python
import signal


@asynccontextmanager
async def lifespan(app: FastAPI):
    """Application lifespan: initialize MongoDB/Beanie and clean up on shutdown."""
    logger.info("AI_CBC API starting up", environment=settings.environment)

    app.state.shutting_down = False

    def _handle_signal(signum, frame):
        app.state.shutting_down = True
        logger.warning("shutdown_signal_received", signal=signum)

    signal.signal(signal.SIGTERM, _handle_signal)
    signal.signal(signal.SIGINT, _handle_signal)

    use_memory = os.environ.get("USE_MEMORY_STORE", "").lower() in ("1", "true", "yes")
    ...

    yield

    if _mongo_client is not None:
        # Wait briefly for in-flight requests before closing the connection.
        timeout = settings.api_graceful_shutdown_timeout
        logger.info("graceful_shutdown_waiting", timeout_seconds=timeout)
        await asyncio.sleep(0.5)  # Give event loop a moment to finish pending tasks
        _mongo_client.close()
    logger.info("AI_CBC API shutting down")
```

- [ ] **Step 2: 在 RateLimitMiddleware 前增加关闭检测**

在 `src/aicbc/api/middleware/rate_limit.py` 的 `dispatch` 开头（或新建中间件）增加：

```python
from fastapi import Request
from starlette.middleware.base import BaseHTTPMiddleware
from starlette.responses import JSONResponse


class ShutdownMiddleware(BaseHTTPMiddleware):
    """Return 503 when the application is shutting down."""

    async def dispatch(self, request: Request, call_next):
        shutting_down = getattr(request.app.state, "shutting_down", False)
        if shutting_down and request.url.path not in {"/health", "/metrics"}:
            return JSONResponse(
                status_code=503,
                content={"error": "Service is shutting down"},
            )
        return await call_next(request)
```

在 `src/aicbc/main.py` 中间件注册中，将 `ShutdownMiddleware` 放到最前面：

```python
from aicbc.api.middleware.shutdown import ShutdownMiddleware

app.add_middleware(ShutdownMiddleware)
app.add_middleware(MetricsMiddleware)
...
```

- [ ] **Step 3: 编写测试**

```python
"""Tests for graceful shutdown behavior."""

import pytest
from fastapi import status
from httpx import AsyncClient


@pytest.mark.asyncio
async def test_shutdown_returns_503(client: AsyncClient):
    client.app.state.shutting_down = True
    try:
        response = await client.get("/api/v1/personas")
        assert response.status_code == status.HTTP_503_SERVICE_UNAVAILABLE
    finally:
        client.app.state.shutting_down = False


@pytest.mark.asyncio
async def test_health_still_available_during_shutdown(client: AsyncClient):
    client.app.state.shutting_down = True
    try:
        response = await client.get("/health")
        assert response.status_code == status.HTTP_200_OK
    finally:
        client.app.state.shutting_down = False
```

- [ ] **Step 4: 运行测试**

Run: `uv run pytest tests/test_graceful_shutdown.py -v`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/aicbc/main.py src/aicbc/api/middleware/shutdown.py tests/test_graceful_shutdown.py
git commit -m "perf(api): implement graceful shutdown with 503 draining"
```

---

## Phase 4: 测试验证与 CI 集成

### Task 23: CI 性能基线门禁

**Files:**
- Modify: `.github/workflows/ci.yml`
- Create: `scripts/ci_compare_baseline.py`

- [ ] **Step 1: 编写基线对比脚本**

```python
"""Compare benchmark result against baseline and fail on regression."""

from __future__ import annotations

import json
import sys
from pathlib import Path


def main() -> None:
    current_path = Path(sys.argv[1])
    baseline_path = Path(sys.argv[2])
    qps_threshold = float(sys.argv[3]) if len(sys.argv) > 3 else 0.15
    p95_threshold = float(sys.argv[4]) if len(sys.argv) > 4 else 0.30

    current = json.loads(current_path.read_text(encoding="utf-8"))
    baseline = json.loads(baseline_path.read_text(encoding="utf-8"))

    baseline_by_endpoint = {b["endpoint"]: b for b in baseline}
    failed = False
    for c in current:
        name = c["endpoint"]
        b = baseline_by_endpoint.get(name)
        if b is None:
            continue
        if b.get("rps", 0) > 0:
            qps_drop = (b["rps"] - c["rps"]) / b["rps"]
            if qps_drop > qps_threshold:
                print(f"FAIL {name}: QPS dropped {qps_drop:.1%} (threshold {qps_threshold:.1%})")
                failed = True
        if c.get("p95_ms", 0) > 0 and b.get("p95_ms", 0) > 0:
            p95_increase = (c["p95_ms"] - b["p95_ms"]) / b["p95_ms"]
            if p95_increase > p95_threshold:
                print(f"FAIL {name}: P95 increased {p95_increase:.1%} (threshold {p95_threshold:.1%})")
                failed = True

    if failed:
        sys.exit(1)
    print("Baseline comparison passed")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 在 CI 中新增 performance-baseline job**

在 `.github/workflows/ci.yml` 的 `jobs:` 末尾追加：

```yaml
  # ==========================================================================
  # Stage 5: Performance Baseline (lightweight regression gate)
  # ==========================================================================
  performance-baseline:
    runs-on: ubuntu-22.04
    needs: quality
    if: github.event_name == 'pull_request'
    services:
      mongo:
        image: mongo:6
        ports:
          - 27017:27017
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/uv
            .venv
          key: ${{ runner.os }}-uv-${{ hashFiles('pyproject.toml', 'uv.lock') }}

      - name: Install dependencies
        run: |
          uv venv --clear
          uv pip install -e ".[dev,analysis]"

      - name: Start API server in background
        run: |
          source .venv/bin/activate
          ENVIRONMENT=testing MONGODB_URL=mongodb://localhost:27017/aicbc REDIS_URL=redis://localhost:6379/0 API_WORKERS=1 uvicorn src.aicbc.main:app --host 127.0.0.1 --port 8000 &
          sleep 10

      - name: Run lightweight API benchmark
        run: |
          source .venv/bin/activate
          uv run python scripts/benchmark_api.py --base-url http://127.0.0.1:8000 --duration 10 --output /tmp/benchmark_current.json

      - name: Compare against baseline
        run: |
          source .venv/bin/activate
          python scripts/ci_compare_baseline.py /tmp/benchmark_current.json reports/performance/latest-baseline.json 0.15 0.30
```

- [ ] **Step 3: 提交基线报告占位**

```bash
mkdir -p reports/performance
uv run python -c "import json; json.dump([], open('reports/performance/latest-baseline.json','w'))"
```

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/ci.yml scripts/ci_compare_baseline.py reports/performance/latest-baseline.json
git commit -m "ci(workflows): add performance baseline regression gate"
```

---

### Task 24: 端到端验证与最终报告

**Files:**
- Create: `reports/performance/2026-06-23-baseline.md`

- [ ] **Step 1: 生成本地基线报告模板**

```markdown
# AI_CBC 性能基线报告

> **日期**：2026-06-23
> **环境**：Azure B2ats v2（待填充）/ 本地 Docker Compose
> **版本**：v1.0

## 环境信息

| 项目 | 值 |
|------|-----|
| VM 规格 | Azure B2ats v2, 1 GiB RAM |
| Docker 版本 | （待填充） |
| Python 版本 | 3.11 |
| 依赖版本 | （待填充） |

## 测试配置

| 场景 | 并发 | 持续时间 | 数据集规模 |
|------|------|----------|------------|
| API health | 50 | 60s | - |
| Dashboard summary | 50 | 60s | - |
| Studies list | 20 | 60s | - |
| Worker HB | - | - | N=150 |

## 关键指标

| 指标 | 优化前 | 优化后 | 目标 | 是否达标 |
|------|--------|--------|------|----------|
| Dashboard P95 (ms) | - | - | < 800 (命中) | 待测 |
| Studies list P95 (ms) | - | - | < 300 | 待测 |
| Persona async API (ms) | - | - | < 500 | 待测 |
| HB N=150 成功率 | - | - | 100% | 待测 |
| API 内存降低 | - | - | >= 10% | 待测 |

## 火焰图/内存图

- 火焰图路径：`reports/performance/flamegraph.svg`
- Memray 报告路径：（待填充）

## 已识别瓶颈 Top 5

1. （待填充）
2. （待填充）
3. （待填充）
4. （待填充）
5. （待填充）
```

- [ ] **Step 2: 更新文档索引**

在 `docs/文档索引与导航.md` 的变更日志区追加 v1.17：

```markdown
- v1.17 (2026-06-23): 新增后端性能优化实施计划 `docs/superpowers/plans/2026-06-23-后端性能优化.md` 与性能基线报告模板 `reports/performance/2026-06-23-baseline.md`。
```

- [ ] **Step 3: Commit**

```bash
git add reports/performance/2026-06-23-baseline.md docs/文档索引与导航.md
git commit -m "docs(perf): add performance baseline report template and index entry"
```

---

## Self-Review

### Spec Coverage

| 设计章节 | 对应任务 |
|----------|----------|
| 2.1 基准脚本 | Task 1, 2, 3 |
| 2.2 分析工具 | Task 3 (py-spy), worker 脚本 |
| 2.3 基线报告 | Task 24 |
| 2.4 CI 集成 | Task 23 |
| 3.1 Dashboard 聚合 | Task 10, 11 |
| 3.2 画像异步化 | Task 12, 13, 14, 15 |
| 3.3 LLM AsyncClient | Task 16 |
| 3.4 MongoDB 投影/连接池 | Task 10, 17 |
| 3.5 缓存策略 | Task 18 |
| 3.6 轻量优化 | Task 19 |
| 4.1 Dockerfile target | Task 20 |
| 4.2 资源限制 | Task 21 |
| 4.3 Graceful Shutdown | Task 22 |
| 4.4 环境变量 | Task 8, 9 |
| 5.1-5.5 监控告警 | Task 4, 5, 6, 7 |
| 6.1-6.4 测试 | Task 1-24 中各测试步骤 |

### Placeholder Scan

- 无 TBD/TODO。
- 所有测试步骤包含实际断言。
- 所有配置变更给出具体默认值。

### Type Consistency

- `PersonaGenerationJobDocument` 字段与 API response 模型一致。
- `generate_batch_on_loop` 签名在 ConsumerGeneratorAgent 与 Celery task 中一致。
- `TimedCache` 的新 backend 协议在 memory/Redis 实现中一致实现。

### Known Gaps / Notes

- `run_persona_generation_task` 在 Worker 中调用同步 `store.save(profile)`；因为 Worker 使用 `_run_coro` 会检测 `_worker_loop`，在同步存储方法内部已正确处理。
- 若项目没有 `memory_study_store` 等 fixture，执行 Agent 需根据现有 fixtures 重命名测试。
- Alertmanager webhook 目标 `http://api:8000/api/v1/admin/alerts` 若不存在，仅作为占位；部署前需创建对应接收端点或改为日志/Slack 接收器。

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-06-23-后端性能优化.md`.**

Two execution options:

**1. Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration.

**2. Inline Execution** — Execute tasks in this session using executing-plans, batch execution with checkpoints.

Which approach would you like?
