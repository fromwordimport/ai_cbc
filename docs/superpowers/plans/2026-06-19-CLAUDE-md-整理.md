# CLAUDE.md 整理与测试文件修改规则实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 统一项目内 12 个 `CLAUDE.md` 的格式与交叉引用，消除重复内容，并在根目录 `Core Design Principles` 中新增"未经授权不得修改测试文件"的规则。

**Architecture:** 以根目录 `CLAUDE.md` 为唯一全局规范源，各子目录 `CLAUDE.md` 通过相对链接引用全局内容；新增规则作为核心设计原则之一写入根目录。

**Tech Stack:** Markdown, Git

---

## File Structure

| File | Current Responsibility | Planned Change |
|------|------------------------|----------------|
| `CLAUDE.md` | 全局仓库指南：目录索引、核心原则、编辑规范、团队角色、数据格式 | 在 `Core Design Principles` 新增 Test File Integrity；保持全局规范源地位 |
| `docs/CLAUDE.md` | 跨文档目录指南 | 移除与根目录重复的团队角色表和编辑规范，改为引用根目录 |
| `consumer-simulation/CLAUDE.md` | 消费者模拟子系统指南 | 在 `Key Cross-References` 增加根目录引用；检查链接 |
| `cbc-questionnaire-system/CLAUDE.md` | CBC 问卷子系统指南 | 同上 |
| `cbc-analysis-system/CLAUDE.md` | CBC 分析子系统指南 | 同上 |
| `src/CLAUDE.md` | 后端源码指南 | 验证 `Cross-References` 链接，必要时增加指向根目录的引用 |
| `frontend/CLAUDE.md` | 前端源码指南 | 同上 |
| `tests/CLAUDE.md` | 测试套件指南 | 验证链接；在 `Key Conventions` 或 `Cross-References` 中引用新的测试文件完整性规则 |
| `configs/CLAUDE.md` | 运行时配置资产指南 | 验证链接 |
| `docker/CLAUDE.md` | 容器构建指南 | 验证链接 |
| `k8s/CLAUDE.md` | K8s 部署指南 | 验证链接 |
| `scripts/CLAUDE.md` | 脚本目录指南 | 验证链接 |

---

### Task 1: 根目录 `CLAUDE.md` 新增 Test File Integrity 原则

**Files:**
- Modify: `CLAUDE.md:67-72`

- [ ] **Step 1: 修改 `Core Design Principles` 章节**

  使用 Edit 工具将以下内容插入到第 4 条 `Bias Zero-Tolerance` 之后：

  ```markdown
  5. **Test File Integrity**: 不得在用户未明确授权的情况下修改测试文件（包括 `tests/`、`frontend/src/__tests__/` 及任何 `*test*.py`、`*spec*` 等测试相关文件）。若确需改动测试，必须先向用户说明理由并获得授权。
  ```

  修改后的 `Core Design Principles` 应为：

  ```markdown
  ## Core Design Principles

  1. **张力优先 (Tension First)**: Virtual consumers must have internally contradictory traits, not average personalities. Every contradiction requires a psychological narrative explanation.
  2. **Four-Layer Persona Model**: Demographics → Behavior → Psychology → Scenarios. Upper layers explain anomalies in lower layers.
  3. **Statistical Rigor**: HB models must report R-hat and ESS; R-hat > 1.1 means non-convergence. Effects coding is the default. Price coefficients must be negative for WTP calculations.
  4. **Bias Zero-Tolerance**: Virtual consumer preferences must not systematically correlate with protected attributes. All persona batches undergo automated bias auditing.
  5. **Test File Integrity**: 不得在用户未明确授权的情况下修改测试文件（包括 `tests/`、`frontend/src/__tests__/` 及任何 `*test*.py`、`*spec*` 等测试相关文件）。若确需改动测试，必须先向用户说明理由并获得授权。
  ```

- [ ] **Step 2: 验证 Markdown 渲染**

  快速检查：编号连续，中文引号正确，反引号成对。

- [ ] **Step 3: 提交**

  ```bash
  git add CLAUDE.md
  git commit -m "docs(root): add Test File Integrity to Core Design Principles"
  ```

---

### Task 2: `docs/CLAUDE.md` 去除与根目录的重复内容

**Files:**
- Modify: `docs/CLAUDE.md:45-82`

- [ ] **Step 1: 替换 `Editing Conventions` 为引用根目录**

  将 `docs/CLAUDE.md` 中的 `Editing Conventions` 章节：

  ```markdown
  ## Editing Conventions

  - Documents use Markdown with **Chinese as the primary language**.
  - Frontmatter uses Chinese blockquote style (`> **版本**：v1.0`), not YAML.
  - Cross-references use relative paths with `[]()` links.
  - **Update `docs/文档索引与导航.md`** when adding new documents.
  - Parameter tables are authoritative — changing a threshold (e.g., `rhat_max < 1.1`) requires同步 updates in all referencing documents.
  ```

  替换为：

  ```markdown
  ## Editing Conventions

  遵循根目录 `../CLAUDE.md` 中的全局编辑规范。本目录特有的补充：

  - **Update `docs/文档索引与导航.md`** when adding new documents.
  - Parameter tables are authoritative — changing a threshold (e.g., `rhat_max < 1.1`) requires同步 updates in all referencing documents.
  ```

- [ ] **Step 2: 替换 `Role Ownership` 为引用根目录**

  将 `docs/CLAUDE.md` 中的完整 `Role Ownership` 表格替换为：

  ```markdown
  ## Role Ownership

  参见根目录 `../CLAUDE.md` 中的团队角色表。本目录文档主要由 小P、小联、小数、小端、小伦、小测、小验、小维、小控、小前、小培、小安 负责。
  ```

- [ ] **Step 3: 验证**

  使用 Read 工具读取 `docs/CLAUDE.md`，确认不再重复出现团队角色表和完整编辑规范。

- [ ] **Step 4: 提交**

  ```bash
  git add docs/CLAUDE.md
  git commit -m "docs(docs): dedupe role table and editing conventions by linking to root"
  ```

---

### Task 3: 统一三个规范子系统的 `CLAUDE.md` 引用

**Files:**
- Modify: `consumer-simulation/CLAUDE.md:55-62`
- Modify: `cbc-questionnaire-system/CLAUDE.md:34-39`
- Modify: `cbc-analysis-system/CLAUDE.md:37-43`

- [ ] **Step 1: 为 `consumer-simulation/CLAUDE.md` 增加根目录引用**

  在 `Key Cross-References` 列表末尾添加：

  ```markdown
  - `../CLAUDE.md` — global repository guidance, team roles, and editing conventions.
  ```

- [ ] **Step 2: 为 `cbc-questionnaire-system/CLAUDE.md` 增加根目录引用**

  在 `Key Cross-References` 列表末尾添加：

  ```markdown
  - `../CLAUDE.md` — global repository guidance, team roles, and editing conventions.
  ```

- [ ] **Step 3: 为 `cbc-analysis-system/CLAUDE.md` 增加根目录引用**

  在 `Key Cross-References` 列表末尾添加：

  ```markdown
  - `../CLAUDE.md` — global repository guidance, team roles, and editing conventions.
  ```

- [ ] **Step 4: 验证并提交**

  ```bash
  git add consumer-simulation/CLAUDE.md cbc-questionnaire-system/CLAUDE.md cbc-analysis-system/CLAUDE.md
  git commit -m "docs(specs): link subsystem CLAUDE.md files to root guidance"
  ```

---

### Task 4: 统一代码目录 `CLAUDE.md` 引用

**Files:**
- Modify: `src/CLAUDE.md:78-86`
- Modify: `frontend/CLAUDE.md:55-59`
- Modify: `tests/CLAUDE.md:65-69`

- [ ] **Step 1: 验证 `src/CLAUDE.md` 的 `Cross-References`**

  当前列表：

  ```markdown
  - `../consumer-simulation/` — generation subsystem specs.
  - `../cbc-questionnaire-system/` — questionnaire subsystem specs.
  - `../cbc-analysis-system/` — analysis subsystem specs.
  - `../docs/数据字典.md` — data schemas.
  - `../configs/` — prompt template and tag config assets.
  - `../tests/` — test suite conventions.
  - `../scripts/` — dev server and batch simulation scripts.
  ```

  在末尾添加：

  ```markdown
  - `../CLAUDE.md` — global repository guidance and team roles.
  ```

- [ ] **Step 2: 验证 `frontend/CLAUDE.md` 的 `Cross-References`**

  当前列表：

  ```markdown
  - `../src/CLAUDE.md` — backend architecture and API details.
  - `../docs/数据字典.md` — API payload schemas.
  - `../tests/CLAUDE.md` — test suite overview (mostly backend; frontend tests live here).
  ```

  在末尾添加：

  ```markdown
  - `../CLAUDE.md` — global repository guidance and team roles.
  ```

- [ ] **Step 3: 在 `tests/CLAUDE.md` 引用新的 Test File Integrity 规则**

  在 `Key Conventions` 列表末尾添加：

  ```markdown
  - **Test File Integrity**: 任何对测试文件（包括本目录和 `../frontend/src/__tests__/`) 的修改，必须事先获得用户明确授权。详见根目录 `../CLAUDE.md` 的 `Core Design Principles`。
  ```

  同时在 `Cross-References` 末尾添加：

  ```markdown
  - `../CLAUDE.md` — global repository guidance, including the Test File Integrity principle.
  ```

- [ ] **Step 4: 验证并提交**

  ```bash
  git add src/CLAUDE.md frontend/CLAUDE.md tests/CLAUDE.md
  git commit -m "docs(code dirs): link src/frontend/tests CLAUDE.md to root guidance"
  ```

---

### Task 5: 验证运维/配置目录 `CLAUDE.md` 链接

**Files:**
- Modify: `configs/CLAUDE.md:31-35`
- Modify: `docker/CLAUDE.md:43-48`
- Modify: `k8s/CLAUDE.md:91-99`
- Modify: `scripts/CLAUDE.md:26-30`

- [ ] **Step 1: 验证 `configs/CLAUDE.md`**

  当前 `Cross-References`：

  ```markdown
  - `../消费者画像.md` — source-of-truth tag system.
  - `../consumer-simulation/` — generation subsystem specs.
  - `../src/aicbc/generators/profile_generator.py` — consumer of `prompts/persona_generation.txt`.
  ```

  确认 `../消费者画像.md`、`../consumer-simulation/CLAUDE.md`、`../src/aicbc/generators/profile_generator.py` 均存在。若 `profile_generator.py` 路径有变化，修正为实际路径。

  在末尾添加：

  ```markdown
  - `../CLAUDE.md` — global repository guidance and team roles.
  ```

- [ ] **Step 2: 验证 `docker/CLAUDE.md`**

  当前 `Cross-References`：

  ```markdown
  - `../docker-compose.yml` — local compose stack.
  - `../k8s/CLAUDE.md` — Kubernetes production deployment.
  - `../src/CLAUDE.md` — backend source conventions.
  - `../docs/CI-CD流水线设计.md` — CI/CD design.
  ```

  确认 `../docker-compose.yml`、`../k8s/CLAUDE.md`、`../src/CLAUDE.md`、`../docs/CI-CD流水线设计.md` 均存在。全部存在。

  在末尾添加：

  ```markdown
  - `../CLAUDE.md` — global repository guidance and team roles.
  ```

- [ ] **Step 3: 验证 `k8s/CLAUDE.md`**

  当前 `Cross-References`：

  ```markdown
  - `../docker/CLAUDE.md` — container build assets.
  - `../docker-compose.yml` — local compose stack.
  - `../.github/workflows/ci.yml` — continuous integration (quality gates, tests, frontend, K8s validation)
  - `../.github/workflows/cd-staging.yml` — staging build and deployment
  - `../.github/workflows/cd-production.yml` — production deployment (manual approval)
  - `../.github/workflows/security-scan.yml` — scheduled security scans
  - `../.github/workflows/nightly.yml` — scheduled full tests and performance checks
  ```

  确认上述文件均存在。在末尾添加：

  ```markdown
  - `../CLAUDE.md` — global repository guidance and team roles.
  ```

- [ ] **Step 4: 验证 `scripts/CLAUDE.md`**

  当前 `Cross-References`：

  ```markdown
  - `../src/CLAUDE.md` — backend source conventions.
  - `../frontend/CLAUDE.md` — frontend dev server usage.
  - `../tests/CLAUDE.md` — test conventions.
  ```

  全部存在。在末尾添加：

  ```markdown
  - `../CLAUDE.md` — global repository guidance and team roles.
  ```

- [ ] **Step 5: 验证并提交**

  ```bash
  git add configs/CLAUDE.md docker/CLAUDE.md k8s/CLAUDE.md scripts/CLAUDE.md
  git commit -m "docs(ops): add root guidance links and verify cross-references"
  ```

---

### Task 6: 全局链接验证

**Files:**
- Create: `scripts/verify_claude_md_links.py`

- [ ] **Step 1: 编写链接验证脚本**

  创建 `scripts/verify_claude_md_links.py`：

  ```python
  #!/usr/bin/env python3
  """Verify that all relative Markdown links in project CLAUDE.md files resolve."""

  from __future__ import annotations

  import re
  import sys
  from pathlib import Path

  REPO_ROOT = Path(__file__).resolve().parent.parent
  CLAUDE_FILES = [
      REPO_ROOT / "CLAUDE.md",
      REPO_ROOT / "consumer-simulation" / "CLAUDE.md",
      REPO_ROOT / "cbc-questionnaire-system" / "CLAUDE.md",
      REPO_ROOT / "cbc-analysis-system" / "CLAUDE.md",
      REPO_ROOT / "docs" / "CLAUDE.md",
      REPO_ROOT / "src" / "CLAUDE.md",
      REPO_ROOT / "frontend" / "CLAUDE.md",
      REPO_ROOT / "tests" / "CLAUDE.md",
      REPO_ROOT / "configs" / "CLAUDE.md",
      REPO_ROOT / "docker" / "CLAUDE.md",
      REPO_ROOT / "k8s" / "CLAUDE.md",
      REPO_ROOT / "scripts" / "CLAUDE.md",
  ]

  LINK_RE = re.compile(r"\[([^\]]+)\]\(([^)]+)\)")

  failures: list[tuple[Path, str, Path]] = []

  for md_file in CLAUDE_FILES:
      text = md_file.read_text(encoding="utf-8")
      for _, target in LINK_RE.findall(text):
          if target.startswith(("http://", "https://", "mailto:")):
              continue
          if target.startswith("#"):
              continue
          resolved = (md_file.parent / target).resolve()
          if not resolved.exists():
              failures.append((md_file, target, resolved))

  if failures:
      print("Broken relative links:")
      for md_file, target, resolved in failures:
          print(f"  {md_file.relative_to(REPO_ROOT)} -> {target} (resolved: {resolved.relative_to(REPO_ROOT)})")
      sys.exit(1)

  print("All relative links in CLAUDE.md files resolve successfully.")
  ```

- [ ] **Step 2: 运行脚本并修复失效链接**

  ```bash
  uv run python scripts/verify_claude_md_links.py
  ```

  预期：如果所有链接有效，输出 "All relative links in CLAUDE.md files resolve successfully."。
  如果有失效链接，根据输出使用 Edit 工具修正对应文件中的链接。

- [ ] **Step 3: 提交**

  ```bash
  git add scripts/verify_claude_md_links.py
  git commit -m "chore(scripts): add CLAUDE.md link verifier"
  ```

---

### Task 7: 最终审查与提交

- [ ] **Step 1: 运行链接验证脚本**

  ```bash
  uv run python scripts/verify_claude_md_links.py
  ```

  预期：PASS

- [ ] **Step 2: 查看完整 diff**

  ```bash
  git diff --stat
  git diff CLAUDE.md
  git diff docs/CLAUDE.md
  ```

  确认：
  - 根目录 `CLAUDE.md` 新增第 5 条核心原则。
  - `docs/CLAUDE.md` 不再包含完整团队角色表和完整编辑规范。
  - 所有子目录 `CLAUDE.md` 增加了指向根目录的引用。
  - 没有误删或语义变化。

- [ ] **Step 3: 最终提交（如尚未提交）**

  如果所有改动都已分步提交，此步骤可跳过。若仍有未提交改动：

  ```bash
  git add -A
  git commit -m "docs: standardize CLAUDE.md cross-references and add Test File Integrity principle"
  ```

---

## Self-Review

**1. Spec coverage:**
- 统一格式模板：通过 Task 3-5 为所有子目录增加根目录引用，保持结构一致。
- 修复交叉引用：通过 Task 6 的链接验证脚本覆盖。
- 合并重复内容：通过 Task 2 移除 `docs/CLAUDE.md` 中重复的团队角色表和编辑规范。
- 新增核心原则：通过 Task 1 在根目录 `Core Design Principles` 添加。

**2. Placeholder scan:**
- 无 TBD/TODO。
- 无 "add appropriate error handling" 等模糊描述。
- 每个步骤均包含具体文件路径和修改内容。

**3. Type consistency:**
- 所有新增链接均使用相对路径 `../CLAUDE.md`，与现有链接风格一致。
- 所有文件路径均基于已读取的实际内容。

**Gap:** 无。

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-06-19-CLAUDE-md-整理.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
