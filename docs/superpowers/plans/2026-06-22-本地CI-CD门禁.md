# 本地 CI/CD 门禁 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现本地 CI/CD 门禁系统，让开发者在 push 前即可跑通 lint、test、frontend、security 等检查，减少 GitHub Actions 无效运行。

**Architecture:** 新增 Python 编排器 `scripts/local_ci.py` 统一调度各 stage；新增 `scripts/check_commit_msg.py` 复用 conventional commit 校验；通过 `.pre-commit-config.yaml` 在 commit-msg / pre-commit / pre-push 三层触发；输出报告到 `local-ci-reports/` 目录。

**Tech Stack:** Python 3.11, uv, subprocess, dataclasses, pre-commit, pytest

---

## 文件结构

| 文件 | 类型 | 职责 |
|------|------|------|
| `scripts/local_ci.py` | 新建 | 本地 CI 编排器，含所有 stage 实现 |
| `scripts/check_commit_msg.py` | 新建 | commit message 格式校验，供 hook 和 local_ci 复用 |
| `.pre-commit-config.yaml` | 修改 | 新增 commit-msg hook 和 pre-push hook |
| `docs/本地CI使用指南.md` | 新建 | 安装、运行、跳过参数、故障排查 |
| `tests/unit/test_local_ci.py` | 新建 | local_ci 单元测试 |
| `tests/integration/test_local_ci_integration.py` | 新建 | local_ci 集成测试 |

---

## Task 1: 创建 commit message 校验器

**Files:**
- Create: `scripts/check_commit_msg.py`
- Test: 手动运行

- [ ] **Step 1: 写入脚本**

```python
#!/usr/bin/env python3
"""Validate AI_CBC conventional commit messages.

Usage:
    python scripts/check_commit_msg.py <commit-msg-file>
    python scripts/check_commit_msg.py --from-git
"""

from __future__ import annotations

import argparse
import re
import subprocess
import sys
from pathlib import Path

VALID_TYPES = r"feat|fix|docs|test|refactor|perf|security|cost|chore|ci"
PATTERN = rf"^({VALID_TYPES})\(.+\):\s.+"


def get_message_from_file(path: Path) -> str:
    text = path.read_text(encoding="utf-8")
    return text.splitlines()[0] if text else ""


def get_message_from_git() -> str:
    result = subprocess.run(
        ["git", "log", "--format=%s", "-n", "1", "HEAD"],
        capture_output=True,
        text=True,
        check=True,
    )
    return result.stdout.strip()


def validate(message: str) -> tuple[bool, str]:
    if not message:
        return False, "commit message is empty"
    if not re.match(PATTERN, message):
        return (
            False,
            "commit message must match: type(scope): description, "
            f"where type is one of {VALID_TYPES}",
        )
    return True, ""


def main() -> int:
    parser = argparse.ArgumentParser()
    parser.add_argument("path", nargs="?", help="path to commit message file")
    parser.add_argument("--from-git", action="store_true", help="read last commit from git")
    args = parser.parse_args()

    if args.path:
        message = get_message_from_file(Path(args.path))
    elif args.from_git:
        message = get_message_from_git()
    else:
        parser.print_help()
        return 2

    ok, error = validate(message)
    if not ok:
        print(f"FAIL: {error}")
        print(f"Message: {message!r}")
        return 1

    print(f"OK: {message}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: 测试通过文件参数校验成功**

Run:
```bash
echo "feat(ci): add local ci runner" > /tmp/msg.txt
uv run python scripts/check_commit_msg.py /tmp/msg.txt
```

Expected:
```
OK: feat(ci): add local ci runner
```

- [ ] **Step 3: 测试通过文件参数校验失败**

Run:
```bash
echo "bad message" > /tmp/msg.txt
uv run python scripts/check_commit_msg.py /tmp/msg.txt
```

Expected:
```
FAIL: commit message must match: type(scope): description, ...
Message: 'bad message'
```

Exit code should be 1.

- [ ] **Step 4: 提交**

```bash
git add scripts/check_commit_msg.py
git commit -m "feat(ci): add commit message validator"
```

---

## Task 2: 创建 local_ci.py 骨架与 CLI

**Files:**
- Create: `scripts/local_ci.py`

- [ ] **Step 1: 写入 CLI 骨架**

```python
#!/usr/bin/env python3
"""Local CI/CD gate runner for AI_CBC."""

from __future__ import annotations

import argparse
import shutil
import subprocess
import sys
import time
from dataclasses import dataclass, field
from pathlib import Path
from typing import Optional

REPO_ROOT = Path(__file__).resolve().parent.parent
REPORTS_DIR_DEFAULT = REPO_ROOT / "local-ci-reports"

STAGES = ["preflight", "lint", "test", "redteam", "frontend", "security", "k8s", "docker", "trivy"]
FAST_STAGES = ["preflight", "lint", "test", "redteam", "frontend", "security"]


@dataclass
class StageResult:
    name: str
    success: bool
    duration: float
    returncode: int = 0
    stdout: str = ""
    stderr: str = ""
    report_files: list[Path] = field(default_factory=list)


class LocalCI:
    def __init__(self, report_dir: Path, verbose: bool = False):
        self.report_dir = report_dir
        self.verbose = verbose
        self.report_dir.mkdir(parents=True, exist_ok=True)

    def log(self, message: str) -> None:
        if self.verbose:
            print(message)

    def run_command(
        self,
        cmd: list[str],
        cwd: Optional[Path] = None,
        env: Optional[dict[str, str]] = None,
        timeout: Optional[int] = None,
    ) -> subprocess.CompletedProcess[str]:
        self.log(f"$ {' '.join(cmd)}")
        return subprocess.run(
            cmd,
            cwd=cwd or REPO_ROOT,
            env=env,
            capture_output=True,
            text=True,
            timeout=timeout,
        )

    def run_stage(self, name: str) -> StageResult:
        method = getattr(self, f"stage_{name}", None)
        if method is None:
            return StageResult(
                name=name,
                success=False,
                duration=0.0,
                stderr=f"Unknown stage: {name}",
            )
        start = time.time()
        try:
            result = method()
        except Exception as exc:  # noqa: BLE001
            result = StageResult(
                name=name,
                success=False,
                duration=time.time() - start,
                stderr=str(exc),
            )
        return result

    def run(self, stages: list[str], fail_fast: bool = False) -> bool:
        results: list[StageResult] = []
        overall = True
        for name in stages:
            result = self.run_stage(name)
            results.append(result)
            if not result.success:
                overall = False
                if fail_fast:
                    break
        self.print_summary(results)
        return overall

    def print_summary(self, results: list[StageResult]) -> None:
        print("\n" + "=" * 40)
        print("Local CI Summary")
        print("=" * 40)
        for r in results:
            icon = "✅" if r.success else "❌"
            extra = f" ({r.returncode})" if not r.success else ""
            print(f"{icon} {r.name:12} {r.duration:6.1f}s{extra}")
        print("-" * 40)
        print(f"Result: {'PASSED' if all(r.success for r in results) else 'FAILED'}")
        print(f"Report dir: {self.report_dir}")


def main() -> int:
    parser = argparse.ArgumentParser(description="Run AI_CBC CI gates locally")
    parser.add_argument(
        "command",
        choices=["lint", "test", "redteam", "frontend", "security", "k8s", "all"],
        help="which stage to run",
    )
    parser.add_argument("--fast", action="store_true", help="skip docker/trivy/k8s")
    parser.add_argument("--full", action="store_true", help="run all stages including docker/trivy/k8s")
    parser.add_argument("--fail-fast", action="store_true", help="stop on first failure")
    parser.add_argument("--verbose", "-v", action="store_true", help="verbose output")
    parser.add_argument("--report-dir", type=Path, default=REPORTS_DIR_DEFAULT)
    parser.add_argument("--skip-trivy", action="store_true")
    parser.add_argument("--skip-tests", action="store_true")
    parser.add_argument("--skip-frontend", action="store_true")
    parser.add_argument("--skip-k8s", action="store_true")
    parser.add_argument("--skip-pip-audit", action="store_true")
    parser.add_argument("--skip-redteam", action="store_true")
    parser.add_argument("--skip-secrets", action="store_true")
    args = parser.parse_args()

    if args.command == "all":
        stages = FAST_STAGES.copy()
        if args.full:
            stages.extend(["docker", "trivy", "k8s"])
    else:
        stages = [args.command]

    skip_map = {
        "trivy": args.skip_trivy,
        "test": args.skip_tests,
        "frontend": args.skip_frontend,
        "k8s": args.skip_k8s,
        "security": args.skip_pip_audit,
        "redteam": args.skip_redteam,
        "preflight": args.skip_secrets,
    }
    stages = [s for s in stages if not skip_map.get(s, False)]

    ci = LocalCI(report_dir=args.report_dir, verbose=args.verbose)
    ok = ci.run(stages, fail_fast=args.fail_fast)
    return 0 if ok else 1


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: 测试 CLI 帮助信息**

Run:
```bash
uv run python scripts/local_ci.py --help
```

Expected: shows help with all subcommands and options.

- [ ] **Step 3: 测试未知 stage 报错**

Run:
```bash
uv run python scripts/local_ci.py all --fast
```

Expected: 所有 stage 方法尚未实现，每个返回 "Unknown stage" 或失败，最终 exit code 非零。

- [ ] **Step 4: 提交**

```bash
git add scripts/local_ci.py
git commit -m "feat(ci): scaffold local ci runner"
```

---

## Task 3: 实现环境检测辅助函数

**Files:**
- Modify: `scripts/local_ci.py`

- [ ] **Step 1: 在 `LocalCI` 类中添加环境检测方法**

在 `__init__` 后添加：

```python
    def require_uv(self) -> None:
        if shutil.which("uv") is None:
            raise RuntimeError("uv is required. Install from https://docs.astral.sh/uv/")

    def require_npm(self) -> None:
        if shutil.which("npm") is None:
            raise RuntimeError("npm is required for frontend stage")

    def require_docker(self) -> None:
        if shutil.which("docker") is None:
            raise RuntimeError("docker is required for docker/trivy stages")
```

- [ ] **Step 2: 在 `main()` 最开头调用 `require_uv()`**

```python
def main() -> int:
    # 先解析 --help 不需要 uv，所以这里不检测；实际在解析后检测
    args = parser.parse_args()
    LocalCI(report_dir=args.report_dir, verbose=args.verbose).require_uv()
    ...
```

更精确的方式：

```python
def main() -> int:
    parser = argparse.ArgumentParser(...)
    # ... add arguments ...
    args = parser.parse_args()

    LocalCI(report_dir=args.report_dir, verbose=args.verbose).require_uv()
    ...
```

- [ ] **Step 3: 测试 uv 缺失提示**

临时把 uv 重命名或PATH不包含 uv 的情况难以模拟，跳过；验证正常路径即可。

Run:
```bash
uv run python scripts/local_ci.py lint
```

Expected: 进入 lint stage（可能失败因为未实现），不会报 uv 缺失。

- [ ] **Step 4: 提交**

```bash
git add scripts/local_ci.py
git commit -m "feat(ci): add environment checks"
```

---

## Task 4: 实现 `run_command` 与报告目录辅助

**Files:**
- Modify: `scripts/local_ci.py`

- [ ] **Step 1: 添加报告文件写入辅助方法**

在 `LocalCI` 类中添加：

```python
    def stage_dir(self, name: str) -> Path:
        d = self.report_dir / name
        d.mkdir(parents=True, exist_ok=True)
        return d

    def write_report(self, stage: str, filename: str, content: str) -> Path:
        d = self.stage_dir(stage)
        p = d / filename
        p.write_text(content, encoding="utf-8")
        return p
```

- [ ] **Step 2: 修改 `run_command` 捕获输出到报告文件**

保持 `run_command` 返回 `subprocess.CompletedProcess`，由 stage 决定是否写入报告。

- [ ] **Step 3: 提交**

```bash
git add scripts/local_ci.py
git commit -m "feat(ci): add report directory helpers"
```

---

## Task 5: 实现 preflight stage

**Files:**
- Modify: `scripts/local_ci.py`
- Reuse: `scripts/check_commit_msg.py`

- [ ] **Step 1: 在文件顶部导入 re**

```python
import re
```

- [ ] **Step 2: 添加 preflight stage 方法**

在 `LocalCI` 类中添加：

```python
    def stage_preflight(self) -> StageResult:
        start = time.time()
        errors: list[str] = []
        report_files: list[Path] = []

        # branch naming
        branch_res = self.run_command(["git", "rev-parse", "--abbrev-ref", "HEAD"])
        branch = branch_res.stdout.strip()
        if not re.match(r"^(master|release/.*|hotfix/.*|feature/.*|worktree/.*|worktree-.*)$", branch):
            errors.append(f"branch naming violation: {branch}")

        # commit message
        commit_res = self.run_command(["git", "log", "--format=%s", "-n", "1", "HEAD"])
        message = commit_res.stdout.strip()
        cm_res = self.run_command(
            [sys.executable, str(REPO_ROOT / "scripts" / "check_commit_msg.py"), "--from-git"]
        )
        if cm_res.returncode != 0:
            errors.append("commit message format violation")

        # secrets scan
        if shutil.which("trufflehog"):
            out_path = self.write_report("preflight", "trufflehog.json", "")
            res = self.run_command(
                ["trufflehog", "git", "file://.", "--only-verified", "--json"],
                timeout=120,
            )
            out_path.write_text(res.stdout, encoding="utf-8")
            report_files.append(out_path)
            if res.returncode != 0:
                errors.append("TruffleHog found verified secrets")
        else:
            errors.append("trufflehog not installed; skipping secrets scan")

        duration = time.time() - start
        return StageResult(
            name="preflight",
            success=len(errors) == 0,
            duration=duration,
            stdout=f"branch: {branch}\nmessage: {message}",
            stderr="\n".join(errors),
            report_files=report_files,
        )
```

- [ ] **Step 3: 测试 preflight**

Run:
```bash
uv run python scripts/local_ci.py all --fast --fail-fast
```

Expected: preflight 显示 ✅ 或 ❌；若当前分支是 master 且最新 commit 格式正确，则通过。

- [ ] **Step 4: 提交**

```bash
git add scripts/local_ci.py
git commit -m "feat(ci): implement preflight stage"
```

---

## Task 6: 实现 lint stage

**Files:**
- Modify: `scripts/local_ci.py`

- [ ] **Step 1: 添加 lint stage 方法**

```python
    def stage_lint(self) -> StageResult:
        start = time.time()
        errors: list[str] = []
        report_files: list[Path] = []
        stage_dir = self.stage_dir("lint")

        # ruff check
        res = self.run_command(["uv", "run", "ruff", "check", "src/", "--output-format=github"])
        if res.returncode != 0:
            errors.append("ruff check failed")

        # ruff format check
        res = self.run_command(["uv", "run", "ruff", "format", "--check", "src/"])
        if res.returncode != 0:
            errors.append("ruff format check failed")

        # mypy (continue on error in CI, but local we still report)
        res = self.run_command(["uv", "run", "mypy", "src/", "--ignore-missing-imports"])
        mypy_path = stage_dir / "mypy-report.txt"
        mypy_path.write_text(res.stdout + res.stderr, encoding="utf-8")
        report_files.append(mypy_path)
        if res.returncode != 0:
            errors.append("mypy found issues (non-blocking in CI, but reported locally)")

        # bandit
        bandit_path = stage_dir / "bandit-report.json"
        res = self.run_command(
            ["uv", "run", "bandit", "-r", "src/", "-f", "json", "-o", str(bandit_path), "--severity-level", "high"]
        )
        report_files.append(bandit_path)
        if res.returncode != 0:
            errors.append("bandit found HIGH severity issues")

        duration = time.time() - start
        return StageResult(
            name="lint",
            success=len(errors) == 0,
            duration=duration,
            stdout="",
            stderr="\n".join(errors),
            report_files=report_files,
        )
```

- [ ] **Step 2: 测试 lint**

Run:
```bash
uv run python scripts/local_ci.py lint
```

Expected: 根据当前代码状态，显示通过或失败，并生成 `local-ci-reports/lint/bandit-report.json` 等。

- [ ] **Step 3: 提交**

```bash
git add scripts/local_ci.py
git commit -m "feat(ci): implement lint stage"
```

---

## Task 7: 实现 test stage

**Files:**
- Modify: `scripts/local_ci.py`

- [ ] **Step 1: 添加 test stage 方法**

```python
    def stage_test(self) -> StageResult:
        start = time.time()
        stage_dir = self.stage_dir("test")
        junit_path = stage_dir / "fast.xml"
        coverage_xml = stage_dir / "coverage.xml"

        cmd = [
            "uv", "run", "pytest", "tests/",
            "-m", "(unit or integration) and not slow and not redteam and not performance and not smoke",
            "--timeout=120",
            "--cov=src",
            "--cov-report=xml:" + str(coverage_xml),
            "--cov-report=html:" + str(stage_dir / "htmlcov"),
            "--cov-fail-under=60",
            f"--junitxml={junit_path}",
        ]
        res = self.run_command(cmd, timeout=600)

        return StageResult(
            name="test",
            success=res.returncode == 0,
            duration=time.time() - start,
            stdout=res.stdout[-2000:] if len(res.stdout) > 2000 else res.stdout,
            stderr=res.stderr[-2000:] if len(res.stderr) > 2000 else res.stderr,
            report_files=[junit_path, coverage_xml],
        )
```

- [ ] **Step 2: 测试 test stage**

Run:
```bash
uv run python scripts/local_ci.py test
```

Expected: 运行 fast 子集测试，生成报告文件。

- [ ] **Step 3: 提交**

```bash
git add scripts/local_ci.py
git commit -m "feat(ci): implement test stage"
```

---

## Task 8: 实现 redteam stage

**Files:**
- Modify: `scripts/local_ci.py`
- Reuse: `scripts/check_defense_rate.py`

- [ ] **Step 1: 添加 redteam stage 方法**

```python
    def stage_redteam(self) -> StageResult:
        start = time.time()
        stage_dir = self.stage_dir("redteam")
        junit_path = stage_dir / "redteam-fast.xml"

        cmd = [
            "uv", "run", "pytest", "tests/redteam/",
            "-v",
            "-m", "security and not slow",
            "--timeout=120",
            f"--junitxml={junit_path}",
        ]
        res = self.run_command(cmd, timeout=600)

        if res.returncode == 0:
            defense = self.run_command(
                [sys.executable, str(REPO_ROOT / "scripts" / "check_defense_rate.py"), str(junit_path), "--threshold", "0.95"]
            )
            success = defense.returncode == 0
        else:
            success = False

        return StageResult(
            name="redteam",
            success=success,
            duration=time.time() - start,
            stdout=res.stdout[-2000:] if len(res.stdout) > 2000 else res.stdout,
            stderr=res.stderr[-2000:] if len(res.stderr) > 2000 else res.stderr,
            report_files=[junit_path],
        )
```

- [ ] **Step 2: 测试 redteam stage**

Run:
```bash
uv run python scripts/local_ci.py redteam
```

Expected: 运行 redteam fast 子集。

- [ ] **Step 3: 提交**

```bash
git add scripts/local_ci.py
git commit -m "feat(ci): implement redteam stage"
```

---

## Task 9: 实现 frontend stage

**Files:**
- Modify: `scripts/local_ci.py`

- [ ] **Step 1: 添加 frontend stage 方法**

```python
    def stage_frontend(self) -> StageResult:
        start = time.time()
        self.require_npm()
        errors: list[str] = []
        report_files: list[Path] = []
        stage_dir = self.stage_dir("frontend")
        frontend_dir = REPO_ROOT / "frontend"

        # npm ci
        res = self.run_command(["npm", "ci"], cwd=frontend_dir, timeout=300)
        if res.returncode != 0:
            errors.append("npm ci failed")

        # tsc
        res = self.run_command(["npx", "tsc", "--noEmit"], cwd=frontend_dir, timeout=120)
        if res.returncode != 0:
            errors.append("tsc type check failed")

        # vitest with coverage
        res = self.run_command(["npm", "run", "test:coverage"], cwd=frontend_dir, timeout=300)
        if res.returncode != 0:
            errors.append("frontend tests failed")

        # build
        res = self.run_command(["npm", "run", "build"], cwd=frontend_dir, timeout=300)
        if res.returncode != 0:
            errors.append("frontend build failed")

        # npm audit
        audit_path = stage_dir / "npm-audit.json"
        res = self.run_command(["npm", "audit", "--audit-level=high", "--json"], cwd=frontend_dir)
        audit_path.write_text(res.stdout if res.stdout else "{}", encoding="utf-8")
        report_files.append(audit_path)
        if res.returncode != 0:
            errors.append("npm audit found HIGH/CRITICAL vulnerabilities")

        # copy coverage report
        cov_src = frontend_dir / "coverage"
        cov_dst = stage_dir / "coverage"
        if cov_src.exists():
            import shutil as _shutil
            _shutil.copytree(cov_src, cov_dst, dirs_exist_ok=True)
            report_files.append(cov_dst)

        duration = time.time() - start
        return StageResult(
            name="frontend",
            success=len(errors) == 0,
            duration=duration,
            stdout="",
            stderr="\n".join(errors),
            report_files=report_files,
        )
```

- [ ] **Step 2: 测试 frontend stage**

Run:
```bash
cd frontend && npm install
uv run python scripts/local_ci.py frontend
```

Expected: 运行前端检查，生成报告。

- [ ] **Step 3: 提交**

```bash
git add scripts/local_ci.py
git commit -m "feat(ci): implement frontend stage"
```

---

## Task 10: 实现 security stage (pip-audit)

**Files:**
- Modify: `scripts/local_ci.py`

- [ ] **Step 1: 添加 security stage 方法**

```python
    def stage_security(self) -> StageResult:
        start = time.time()
        stage_dir = self.stage_dir("security")
        audit_path = stage_dir / "pip-audit.json"

        # ensure pip-audit is available
        res = self.run_command(["uv", "run", "pip", "install", "pip-audit"], timeout=120)
        # pip-audit exits non-zero when vulnerabilities found
        res = self.run_command(
            ["uv", "run", "pip-audit", "--desc", "--format=json", "-o", str(audit_path)],
            timeout=300,
        )
        audit_path.write_text(audit_path.read_text(encoding="utf-8") if audit_path.exists() else "[]", encoding="utf-8")

        check = self.run_command(
            [sys.executable, str(REPO_ROOT / "scripts" / "ci_check_pip_audit.py")]
        )

        return StageResult(
            name="security",
            success=check.returncode == 0,
            duration=time.time() - start,
            stdout=res.stdout[-2000:] if len(res.stdout) > 2000 else res.stdout,
            stderr=res.stderr[-2000:] if len(res.stderr) > 2000 else res.stderr,
            report_files=[audit_path],
        )
```

- [ ] **Step 2: 测试 security stage**

Run:
```bash
uv run python scripts/local_ci.py security
```

Expected: 运行 pip-audit，生成 `local-ci-reports/security/pip-audit.json`。

- [ ] **Step 3: 提交**

```bash
git add scripts/local_ci.py
git commit -m "feat(ci): implement security stage"
```

---

## Task 11: 实现 k8s stage

**Files:**
- Modify: `scripts/local_ci.py`

- [ ] **Step 1: 添加 k8s stage 方法**

```python
    def stage_k8s(self) -> StageResult:
        start = time.time()
        stage_dir = self.stage_dir("k8s")
        log_path = stage_dir / "k8s-validation.log"

        res = self.run_command(["uv", "run", "python", "scripts/validate_k8s_manifests.py"])
        log_path.write_text(res.stdout + res.stderr, encoding="utf-8")

        return StageResult(
            name="k8s",
            success=res.returncode == 0,
            duration=time.time() - start,
            stdout=res.stdout,
            stderr=res.stderr,
            report_files=[log_path],
        )
```

- [ ] **Step 2: 测试 k8s stage**

Run:
```bash
uv run python scripts/local_ci.py k8s
```

Expected: 运行 K8s 校验脚本。

- [ ] **Step 3: 提交**

```bash
git add scripts/local_ci.py
git commit -m "feat(ci): implement k8s stage"
```

---

## Task 12: 实现 docker 和 trivy stages

**Files:**
- Modify: `scripts/local_ci.py`

- [ ] **Step 1: 添加 docker stage 方法**

```python
    def stage_docker(self) -> StageResult:
        start = time.time()
        self.require_docker()
        stage_dir = self.stage_dir("docker")
        log_path = stage_dir / "build.log"

        res = self.run_command(
            ["docker", "build", "-f", "docker/Dockerfile", "-t", "aicbc-scan:local", "."],
            timeout=600,
        )
        log_path.write_text(res.stdout + res.stderr, encoding="utf-8")

        return StageResult(
            name="docker",
            success=res.returncode == 0,
            duration=time.time() - start,
            stdout=res.stdout[-1000:] if len(res.stdout) > 1000 else res.stdout,
            stderr=res.stderr[-1000:] if len(res.stderr) > 1000 else res.stderr,
            report_files=[log_path],
        )
```

- [ ] **Step 2: 添加 trivy stage 方法**

```python
    def stage_trivy(self) -> StageResult:
        start = time.time()
        self.require_docker()
        stage_dir = self.stage_dir("trivy")
        sarif_path = stage_dir / "trivy-results.sarif"

        # check trivy binary
        if shutil.which("trivy") is None:
            return StageResult(
                name="trivy",
                success=False,
                duration=0.0,
                stderr="trivy not installed. Install from https://aquasecurity.github.io/trivy/",
            )

        res = self.run_command(
            [
                "trivy", "image",
                "--severity", "CRITICAL,HIGH",
                "--exit-code", "1",
                "--format", "sarif",
                "--output", str(sarif_path),
                "aicbc-scan:local",
            ],
            timeout=600,
        )

        return StageResult(
            name="trivy",
            success=res.returncode == 0,
            duration=time.time() - start,
            stdout=res.stdout[-1000:] if len(res.stdout) > 1000 else res.stdout,
            stderr=res.stderr[-1000:] if len(res.stderr) > 1000 else res.stderr,
            report_files=[sarif_path],
        )
```

- [ ] **Step 3: 测试（需要 Docker）**

Run:
```bash
uv run python scripts/local_ci.py all --full
```

Expected: 如果 Docker 可用，跑 docker build 和 trivy；否则报错提示安装 Docker。

- [ ] **Step 4: 提交**

```bash
git add scripts/local_ci.py
git commit -m "feat(ci): implement docker and trivy stages"
```

---

## Task 13: 完善 summary 输出与错误详情

**Files:**
- Modify: `scripts/local_ci.py`

- [ ] **Step 1: 修改 `print_summary` 显示失败详情**

```python
    def print_summary(self, results: list[StageResult]) -> None:
        print("\n" + "=" * 50)
        print("Local CI Summary")
        print("=" * 50)
        for r in results:
            icon = "✅" if r.success else "❌"
            print(f"{icon} {r.name:12} {r.duration:6.1f}s")
        print("-" * 50)
        all_ok = all(r.success for r in results)
        print(f"Result: {'PASSED' if all_ok else 'FAILED'}")
        print(f"Report dir: {self.report_dir}")
        if not all_ok:
            print("\nFailed stages details:")
            for r in results:
                if not r.success:
                    print(f"\n--- {r.name} ---")
                    if r.stderr:
                        print(r.stderr)
                    if r.stdout:
                        print(r.stdout)
```

- [ ] **Step 2: 测试 summary**

Run:
```bash
uv run python scripts/local_ci.py lint
```

Expected: 失败时显示具体错误信息。

- [ ] **Step 3: 提交**

```bash
git add scripts/local_ci.py
git commit -m "feat(ci): improve summary output"
```

---

## Task 14: 更新 .pre-commit-config.yaml

**Files:**
- Modify: `.pre-commit-config.yaml`

- [ ] **Step 1: 在文件末尾添加 local hooks**

```yaml
  - repo: local
    hooks:
      - id: commit-message-check
        name: Commit Message Check
        entry: uv run python scripts/check_commit_msg.py
        language: system
        stages: [commit-msg]
        pass_filenames: true

      - id: local-ci-fast
        name: Local CI Fast Gate
        entry: uv run python scripts/local_ci.py all --fast --fail-fast
        language: system
        stages: [pre-push]
        pass_filenames: false
        always_run: true
```

- [ ] **Step 2: 验证 YAML 格式**

Run:
```bash
uv run pre-commit validate-config
```

或：
```bash
python -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))"
```

Expected: 无报错。

- [ ] **Step 3: 提交**

```bash
git add .pre-commit-config.yaml
git commit -m "feat(ci): add commit-msg and pre-push hooks"
```

---

## Task 15: 创建本地 CI 使用指南

**Files:**
- Create: `docs/本地CI使用指南.md`

- [ ] **Step 1: 写入指南**

```markdown
# 本地 CI 使用指南

> **版本**：v1.0
> **定位**：在本地跑通 AI_CBC CI/CD 门禁，减少 GitHub Actions 额度消耗
> **维护者**：小维

---

## 安装依赖

- Python ≥3.11
- uv
- Node.js ≥20
- npm ≥10
- Git
- Docker Desktop（仅完整模式需要）
- Trivy（仅完整模式需要）
- TruffleHog（可选，推荐安装）

## 安装 hooks

```bash
uv run pre-commit install --hook-type pre-commit --hook-type pre-push --hook-type commit-msg
```

## 常用命令

```bash
# 完整本地 CI（含 Docker/Trivy/K8s）
uv run python scripts/local_ci.py all

# 快速模式（默认，不含 Docker/Trivy）
uv run python scripts/local_ci.py all --fast

# 仅跑 lint
uv run python scripts/local_ci.py lint

# 跳过某些 stage
uv run python scripts/local_ci.py all --skip-frontend --skip-trivy

# 失败立即退出
uv run python scripts/local_ci.py all --fast --fail-fast
```

## 报告输出

所有报告默认输出到 `local-ci-reports/`，与 GitHub Actions artifacts 文件名对齐。

## 故障排查

| 问题 | 解决 |
|------|------|
| pre-push 太慢 | 使用 `--skip-*` 参数跳过不关心的 stage |
| trufflehog 未安装 | 从 https://github.com/trufflesecurity/trufflehog 下载 |
| Docker 未安装 | 仅影响 `--full` 模式；快速模式不受影响 |
| npm ci 失败 | 检查 Node.js 版本和网络 |

## 与 GitHub Actions 的关系

本地 CI 是前置门禁，GitHub Actions 仍是最终门禁。本地通过后，推送才会触发线上 CI。
```

- [ ] **Step 2: 提交**

```bash
git add docs/本地CI使用指南.md
git commit -m "docs(ci): add local ci usage guide"
```

---

## Task 16: 创建 local_ci 单元测试（需用户授权新增测试文件）

**Files:**
- Create: `tests/unit/test_local_ci.py`

**Note:** 本任务会新建测试文件。实施前请确认用户已授权。

- [ ] **Step 0: 确认用户授权**

向用户确认："本任务将新建 `tests/unit/test_local_ci.py` 以测试本地 CI 编排器，是否继续？"

- [ ] **Step 1: 写入单元测试**

```python
from __future__ import annotations

import subprocess
import sys
from pathlib import Path
from unittest.mock import MagicMock, patch

import pytest

REPO_ROOT = Path(__file__).resolve().parents[2]
if str(REPO_ROOT / "scripts") not in sys.path:
    sys.path.insert(0, str(REPO_ROOT / "scripts"))

from local_ci import LocalCI, StageResult


@pytest.fixture
def ci(tmp_path: Path) -> LocalCI:
    return LocalCI(report_dir=tmp_path, verbose=False)


def test_stage_dir_created(ci: LocalCI) -> None:
    d = ci.stage_dir("lint")
    assert d.exists()
    assert d.name == "lint"


def test_write_report(ci: LocalCI) -> None:
    p = ci.write_report("lint", "bandit-report.json", "{}")
    assert p.exists()
    assert p.read_text() == "{}"


def test_run_stage_unknown(ci: LocalCI) -> None:
    result = ci.run_stage("nonexistent")
    assert not result.success
    assert "Unknown stage" in result.stderr


def test_print_summary_all_pass(ci: LocalCI, capsys: pytest.CaptureFixture[str]) -> None:
    results = [StageResult(name="lint", success=True, duration=1.0)]
    ci.print_summary(results)
    out = capsys.readouterr().out
    assert "PASSED" in out


def test_print_summary_with_failure(ci: LocalCI, capsys: pytest.CaptureFixture[str]) -> None:
    results = [StageResult(name="lint", success=False, duration=1.0, stderr="ruff failed")]
    ci.print_summary(results)
    out = capsys.readouterr().out
    assert "FAILED" in out
    assert "ruff failed" in out


def test_run_command_returns_completed_process(ci: LocalCI) -> None:
    res = ci.run_command(["python", "-c", "print('hello')"])
    assert res.returncode == 0
    assert "hello" in res.stdout
```

- [ ] **Step 2: 运行单元测试**

Run:
```bash
uv run pytest tests/unit/test_local_ci.py -v
```

Expected: 全部通过。

- [ ] **Step 3: 提交**

```bash
git add tests/unit/test_local_ci.py
git commit -m "test(ci): add unit tests for local ci runner"
```

---

## Task 17: 创建 local_ci 集成测试（需用户授权新增测试文件）

**Files:**
- Create: `tests/integration/test_local_ci_integration.py`

**Note:** 本任务会新建测试文件。实施前请确认用户已授权。

- [ ] **Step 0: 确认用户授权**

向用户确认："本任务将新建 `tests/integration/test_local_ci_integration.py`，是否继续？"

- [ ] **Step 1: 写入集成测试**

```python
from __future__ import annotations

import subprocess
import sys
from pathlib import Path

import pytest

REPO_ROOT = Path(__file__).resolve().parents[2]


@pytest.mark.integration
@pytest.mark.slow
def test_local_ci_lint_runs_without_crash() -> None:
    result = subprocess.run(
        [sys.executable, str(REPO_ROOT / "scripts" / "local_ci.py"), "lint"],
        cwd=REPO_ROOT,
        capture_output=True,
        text=True,
        timeout=300,
    )
    # lint 可能通过也可能失败，但不能崩溃
    assert "Local CI Summary" in result.stdout or "Local CI Summary" in result.stderr


@pytest.mark.integration
def test_check_commit_msg_rejects_bad_message(tmp_path: Path) -> None:
    bad_msg = tmp_path / "bad-msg.txt"
    bad_msg.write_text("bad message\n", encoding="utf-8")
    result = subprocess.run(
        [sys.executable, str(REPO_ROOT / "scripts" / "check_commit_msg.py"), str(bad_msg)],
        capture_output=True,
        text=True,
        timeout=30,
    )
    assert result.returncode != 0
```

- [ ] **Step 2: 运行集成测试**

Run:
```bash
uv run pytest tests/integration/test_local_ci_integration.py -v
```

Expected: 通过或按标记跳过。

- [ ] **Step 3: 提交**

```bash
git add tests/integration/test_local_ci_integration.py
git commit -m "test(ci): add integration tests for local ci runner"
```

---

## Task 18: 手动验证 hooks

**Files:**
- Modify: 无（手动验证）

- [ ] **Step 1: 安装 hooks**

Run:
```bash
uv run pre-commit install --hook-type pre-commit --hook-type pre-push --hook-type commit-msg
```

- [ ] **Step 2: 验证 commit-msg hook**

Run:
```bash
echo "bad message" > /tmp/bad-msg.txt
uv run pre-commit run commit-message-check --hook-stage commit-msg /tmp/bad-msg.txt
```

Expected: 失败，提示 commit message 格式错误。

- [ ] **Step 3: 验证 pre-push hook（可选，耗时较长）**

Run:
```bash
uv run pre-commit run local-ci-fast --hook-stage pre-push
```

Expected: 执行轻量门禁，通过或显示失败。

- [ ] **Step 4: 记录验证结果**

无需提交代码，在 PR 描述中说明已手动验证。

---

## Task 19: 完整运行本地 CI 并修复问题

**Files:**
- Modify: 根据运行结果修复

- [ ] **Step 1: 运行完整 fast 模式**

Run:
```bash
uv run python scripts/local_ci.py all --fast
```

- [ ] **Step 2: 根据失败项修复代码或调整配置**

例如：
- lint 失败 → 运行 `uv run ruff format src/` 和 `uv run ruff check src/ --fix`
- test 失败 → 修复测试或代码
- frontend 失败 → 修复前端代码

- [ ] **Step 3: 运行完整模式（需要 Docker）**

Run:
```bash
uv run python scripts/local_ci.py all
```

- [ ] **Step 4: 提交最终修复**

```bash
git add .
git commit -m "fix(ci): resolve local ci failures"
```

---

## 验收标准

| 检查项 | 通过标准 |
|--------|---------|
| `scripts/local_ci.py all --fast` | 在干净环境下成功完成所有 fast stage |
| `scripts/check_commit_msg.py` | 正确识别符合/不符合规范的 commit message |
| `.pre-commit-config.yaml` | commit-msg 和 pre-push hook 可正常安装运行 |
| 报告输出 | `local-ci-reports/` 下生成对应报告文件 |
| GitHub Actions | 不修改现有 workflow，线上 CI 继续运行 |
| 测试 | 新增单元/集成测试通过（如用户授权创建） |

---

## 自评

### Spec 覆盖检查

| Spec 要求 | 对应 Task |
|-----------|----------|
| Python 编排器 `scripts/local_ci.py` | Task 2-13 |
| commit message 校验 `scripts/check_commit_msg.py` | Task 1 |
| pre-commit hooks 三层触发 | Task 14 |
| `--fast` / `--full` 模式 | Task 2, 12 |
| `--skip-*` 参数 | Task 2 |
| 报告输出到 `local-ci-reports/` | Task 4, 13 |
| 使用指南 | Task 15 |
| 测试 | Task 16-17 |

### Placeholder 检查

- 无 TBD/TODO
- 无 "add appropriate error handling" 等模糊描述
- 每个代码步骤包含实际代码

### 类型一致性检查

- `LocalCI` 类、`StageResult` dataclass 在所有 task 中保持一致
- 命令路径使用 `Path` 对象或字符串一致

---

*Plan complete.*
